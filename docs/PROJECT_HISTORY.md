# Project History

The single history archive for this project. Add a Session Log entry after every session. Add Completed Features / Resolved Items rows when work ships. Add a Milestone Reviews entry only when a rating changes or a major milestone is reached.

---

## Session Log

- 2026-05-05: [refactor] PersistBlock_t sub-struct adoption — PERSIST_VERSION bumped 8→9 (2472 B → 2488 B). Five typed sub-structs added to `persist.h`: `GraphPersist_t` (696 B), `StatPersist_t` (1204 B), `MatrixPersist_t` (440 B), `PrgmPersist_t` (4 B, reserved), `ModePersist_t` (12 B). Per-section version constants added (`GRAPH_PERSIST_VERSION` … `MODE_PERSIST_VERSION`, all 1U). `PersistBlock_t` header changed from flat to `magic(4B)+version(2B)+5×sub_ver(10B)=16B` plus sub-sections plus top-level fields. `Persist_BuildBlock`/`Persist_ApplyBlock`/`Persist_Reset` updated to use sub-struct field paths (e.g. `out.graph.x_min`). `Persist_Save` stamps all sub-versions alongside magic/version. `test_persist_roundtrip.c` updated: `fill_block` uses sub-struct paths, `test_field_roundtrip` uses sub-struct paths, size assertion updated to 2488. 14/14 ctest, 964 assertions, 139/139 check_sync. v8 blocks on FLASH are discarded on first boot (version mismatch); no data-preserving migration for this one-time structural transition. **Complexity delta: `decrease`** (BuildBlock/ApplyBlock now use named sub-struct fields instead of flat field soup; no file line-count increase).
- 2026-05-05: TRACE auto-pan at viewport edge (guidebook p. 3-10) — `TOKEN_LEFT`/`TOKEN_RIGHT` cases in `handle_trace_mode()` (`graph_ui.c`) now pan the viewport by `(Xmax−Xmin)/2` when the cursor reaches `Xmin`/`Xmax` in non-param mode (was: clamped); captures all gs fields before `Graph_SetWindow()` call; `Graph_Render()` called before `Graph_DrawTrace()` on pan; param mode unchanged (bounded by t_min/t_max); 14/14 ctest pass, 964 assertions. **Complexity delta: `neutral`** (two else-branches added to existing cases; no new state or files).
- 2026-05-05: Interactive on-graph DRAW (guidebook pp. 5-2, 5-5/5-6) — `MODE_GRAPH_DRAW_CURSOR` added to `CalcMode_t`; `draw_enter_cursor_pick(op)` and `handle_draw_cursor_mode()` added to `graph_ui.c`/`graph_ui.h`; `Graph_DrawLineCursor()` added to `graph.c`/`graph.h` (restores clean frame, draws optional Bresenham preview line, draws crosshair — same fast path as `Graph_DrawZBox`); `draw_menu_select()` in `ui_draw.c` detects graph context via `is_graph_context()` and routes Line(/PT-On/Off/Chg( to cursor-pick instead of expression-buffer insertion; `pred_graph_mode()` and `Graph_HandleKey()` updated; `ui_draw.c` gains `#include "graph_ui.h"`; `graph_ui.c` gains `#include "graph_draw.h"` + `#include "ui_draw.h"`; 14/14 ctest pass, 964 assertions, 139/139 check_sync. **Complexity delta: `increase`** — `graph_ui.c` gains ~150 lines, pushing past 500-line threshold; [complexity] extraction item added to CLAUDE.md.
- 2026-05-05: ZOOM In/Out/Integer cursor-pick mode — new `MODE_GRAPH_ZOOM_CURSOR` added to `CalcMode_t` (graph cursor modes group); `zoom_enter_cursor_pick(uint8_t op)` and `handle_zoom_cursor_mode()` added to `graph_ui.c`/`graph_ui.h`; reuses `ZBoxState_t` pixel cursor state and `Graph_DrawZBox()` crosshair draw (corner1_set always false); ENTER applies zoom centred on picked pixel and re-renders staying in cursor-pick mode (re-ENTER re-zooms per guidebook); `zoom_scale_view()` static removed (dead); `apply_zoom_preset(5)` ZInteger branch removed (dead); `MODE_GRAPH_ZOOM_CURSOR` added to `pred_graph_mode()` in `calculator_core.c` and `Graph_HandleKey()` switch in `graph.c`; 14/14 ctest pass, 964 assertions. **Complexity delta: `neutral`** (ZBox infrastructure reused; no new draw paths).
- 2026-05-05 (Arch-5): `Graph_HandleKey()` unified graph-mode key dispatcher. `calculator_core.c`'s 8 separate graph-mode entries in `k_route_table[]` replaced by a single predicate-based entry (`pred_graph_mode` + `Graph_HandleKey`). `Graph_HandleKey(Token_t t)` added to `graph.c` (guarded by `#ifndef HOST_TEST`); declared in `graph.h`. `graph.c` gains `#include "graph_ui.h"`, `#include "graph_ui_range.h"`, `#include "ui_graph_zoom.h"` inside the `#ifndef HOST_TEST` block to access the per-mode handlers it delegates to. `calculator_core_test_stubs.h` gains `Graph_HandleKey` stub. Resolves Deepening Opportunity 1 from 2026-04-30 architecture review. Unlocks TRACE auto-pan, ZOOM cursor-pick, `Input` no-arg, and Interactive DRAW as single-file changes to `graph.c`. 14/14 ctest pass, 964 assertions. **Complexity delta: `decrease`** (8 dispatch table entries → 1; `calculator_core.c` no longer enumerates graph sub-modes).
- 2026-05-04 (Arch-1-H): Deleted `calc_internal.h`. Rehomed 6 declarations: `cursor_render`/`cursor_box_create` → `ui_shared.h`; `ui_refresh_display`/`ui_output_row`/`handle_history_nav` → `calculator_core.h`; `zoom_enter_zbox` → `graph_ui.h` (redundant `graph_ui_yeq_insert` declaration removed). Replaced all 10 `#include "calc_internal.h"` sites (6 unconditional, 4 conditional `#else` blocks) with targeted includes. Cleaned stale `calc_internal.h` doc-comments across 14 files. 14/14 ctest pass, 964 assertions. Complexity delta: decrease.
- 2026-03-20: PRGM UI polish, colour palette extraction (`ui_palette.h`), PRGM module extraction to `ui_prgm.c`
- 2026-03-21: `expr_util.c` extraction (9 pure functions), 301-test host suite, persist round-trip tests, HAL guards in `persist.c`, full quality review pass
- 2026-03-21 (Session 6): `graph_ui.c` extraction (P2), float printf runtime guard (P8), FLASH sector map docs (P16)
- 2026-03-21 (Session 7): Integrate project update procedure (workflow, documentation, guidelines)
- 2026-03-21 (Session 8): Implement Global Hard QUIT (2nd+CLEAR) navigation
- 2026-03-21 (Session 9): IDE/Build fixes (IntelliSense header fix, recursive include resolve, debug config fix, CMake build fix)
- 2026-03-22 (Session 10): P6, P12, P13, P17 resolved (Sweet Spot items). -Werror enabled for App; Architecture/Testing/Troubleshooting docs created.
- 2026-03-22 (Session 11): Implement Y= equation enable/disable toggle functionality. Update graph renderer and persistence (v4). Fixed a startup crash and multiple trace/graph transition freezes in `graph.c` and `graph_ui.c` (guards for zero-scale ticks, loop clamping for singularities, and LVGL mutex synchronization).
- 2026-03-22 (Session 12): [P15] Expression pipeline documented in `TECHNICAL.md` with "2 + sin(45)" worked example. `MAINTENANCE_STANDARDS.md` updated.
- 2026-03-22 (Session 13): Matrix history display refactored — column-aligned rows, horizontal scroll via LEFT/RIGHT when expression is empty, `<`/`>` clip indicators. `HistoryEntry_t` now embeds `CalcMatrix_t` copy. Build at 82.44% RAM.
- 2026-03-22 (Session 15): P18 resolved — all 10 CODE_REVIEW_PENDING items complete. PRGM execution logic moved from `ui_prgm.c` to `prgm_exec.c` (−545 L / +540 L). Over-100-line functions split: `ShuntingYard` (3 helpers), `handle_yeq_mode` (navigation+insertion), `ui_init_graph_screens` (4 per-screen), `handle_history_nav` (`commit_history_entry`), `ui_refresh_display` (`render_result_row`), `try_tokenize_number` (2 helpers). Docs: README links, ARCHITECTURE diagram, `calc_internal.h` scope comment. `CODE_REVIEW_PENDING.md` deleted. Function complexity C+ → B. 301/301 tests pass. **Complexity delta: `decrease`**.
- 2026-03-22 (Session 16): Graph render speed — added `MATH_VAR_X` token and `GraphEquation_t` postfix cache API to `calc_engine`. `Graph_Render` now runs Tokenize+ShuntingYard once per equation per render (on equation change only) and calls `Calc_EvalGraphEquation` per pixel column. Parse cost 320× → 1× per equation per frame. 301/301 tests pass. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 17): `HistoryEntry_t` matrix ring buffer refactor — replaced embedded `CalcMatrix_t` (148 B) with 3-byte ring reference. 8-slot `matrix_ring[]` stores last 8 matrix results; generation counter detects eviction and falls back to pre-formatted `result` string. RAM: 82.44% → 81.82%. 301/301 tests pass. **Complexity delta: `decrease`**.
- 2026-03-22 (Session 18): RAM audit (P12) — root cause: LVGL heap + FreeRTOS heap = 128 KB = 65% of 192 KB internal RAM; SDRAM had 63.5 MB free. Fix: `SDRAM` region added to linker script, `.sdram (NOLOAD)` section, `LV_ATTRIBUTE_LARGE_RAM_ARRAY` redirects LVGL heap to SDRAM. RAM: 81.82% → 48.49%. 301/301 tests pass. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 19): PRGM system feature-complete — `IS>(` and `DS<(` implemented; `DispHome`, `DispGraph`, `Output(`, `Menu(` implemented. PRGM: ~50% → ~95% (hardware validation pending, P10). 301/301 tests pass. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 20): PRGM command reference created — `docs/PRGM_COMMANDS.md`. `docs/PRGM_COMPLETION.md` deleted. 301/301 tests pass. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 21): Periodic code review — structural scan + Phase 2 direct reads. P19/P20 opened. VARS/Y-VARS menu specs added to CLAUDE.md. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 22): P19 resolved — `prgm_execute_line` dispatch table refactor. 22 static `cmd_*` handler functions extracted; dispatch table replaces 495-line if/else chain; body reduced to ~30 lines. Zero logic changes. 301/301 tests pass. **Complexity delta: `decrease`**.
- 2026-03-22 (Session 23): P20 resolved — program execution host test suite. `#ifndef HOST_TEST` guards added throughout `prgm_exec.c`/`.h`; 121 tests / 14 groups. Bug fixed: `prgm_run_loop` Stop/Return/Goto-abort path did not reset `current_mode`. Testing B+ → A-. 422/422 tests pass. **Complexity delta: `neutral`**.
- 2026-03-25 (Session 27): PRGM manual test plan rewrite — comprehensive 50-test plan targeting Session 26 regressions. Test count: 40 → 50. **Complexity delta: `neutral`**.
- 2026-03-25 (Session 26): PRGM hardware test fixes — all 5 groups (A–E) executed. CTL menu → 8 items; I/O menu → 5 items; removed Then/Else/While/For/Return/Prompt/Output(/Menu( per spec; EXEC tab execution model implemented. Test suite: 378/378 pass (−44 tests for removed commands). Firmware: 48.45% RAM, 36.28% FLASH. **Complexity delta: `decrease`**.
- 2026-03-26 (Session 28): `QUALITY_TRACKER.md` renamed to `MAINTENANCE_STANDARDS.md`; restructured as process document; history moved to `PROJECT_HISTORY.md`; session log consolidated from `CLAUDE.md`. **Complexity delta: `neutral`**.
- 2026-03-27 (Session 29): PRGM hardware test fix groups F1–F10 — 9 failing tests addressed. F1: CLEAR aborts infinite loop (`prgm_request_abort()` called from keypadTask + `osDelay(0)` per line in `prgm_run_loop`). F2: `DispGraph` deadlock fixed (`osDelay(20)` before `Graph_Render` per gotcha #16). F3: EXEC tab added to in-editor CTL/IO sub-menu — slot picker screen, 3-way tab navigation, subroutine insertion (`prgmNAME`); new `MODE_PRGM_EXEC_MENU`. F4: TEST/MATH menus now accessible from program editor; insert redirected via `prgm_editor_menu_insert()`. F5: Name-entry DEL re-engages ALPHA; ALPHA_LOCK routing fixed. F6/F7: ALPHA+letter and digit slot shortcuts work in all 3 PRGM tabs. F8: INS toggles insert mode in editor; underscore cursor shape for insert. F9: `Input` prompt always shows `?` only. F10: Arrow navigation in name-entry (`prgm_new_name_cursor`); LEFT/RIGHT move in field; DOWN opens editor body; UP at editor line 0 returns to name entry. 378/378 tests pass. Firmware: 48.48% RAM, 36.45% FLASH. **Complexity delta: `increase`** (`ui_prgm.c` grew ~200 lines: new EXEC sub-menu screen + handler + cursor state; see complexity item in CLAUDE.md).
- 2026-03-31 (Session 30): Cursor insert+modifier fix — `LV_OBJ_FLAG_OVERFLOW_VISIBLE` on cursor box; `in_insert` condition simplified (removed `current_mode` guards) so mode indicator (^/A) renders above the underline cursor in insert+2ND/ALPHA. `handle_yeq_insertion` in `graph_ui.c` refactored to use `ExprUtil_InsertChar`/`InsertStr` (−20 lines duplicate logic). `test_prgm_exec` expanded by 18 tests (empty body, slot lookup, 2-level nesting): 378 → 396 total. P26/P27/P28 refactor items added to CLAUDE.md. Full doc-sync pass: test counts corrected in `GETTING_STARTED.md`, `README.md`, `TECHNICAL.md`. **Complexity delta: `decrease`** (graph_ui.c deduplication + calculator_core.c simplification).
- 2026-04-01: P25 resolved — `docs/PRGM_COMMANDS.md` rewritten to match post-Session-26 8-CTL/5-IO spec. Removed Then/Else/While/For(/Return/Prompt/Output(/Menu( entries; updated If (single-line only), Goto/Lbl (single-char constraint), Input (shows `?`), End (no-op note); documented EXEC tab `prgm<name>` insertion model; removed stale Limits row. **Complexity delta: `neutral`**.
- 2026-04-01: `handle_prgm_editor` split — extracted `prgm_editor_handle_nav`, `prgm_editor_handle_del_clear`, `prgm_editor_handle_insert` as three focused static helpers in `ui_prgm.c`. Main dispatcher reduced from 152 → 40 lines. Zero logic change. **Complexity delta: `neutral`**.
- 2026-04-02: `handle_normal_mode` host test suite — 104 tests / 10 groups covering all 8 static sub-handlers. `calculator_core_test_stubs.h` (comprehensive LVGL/RTOS/graph_ui/ui_matrix/ui_prgm stub layer), minimal HOST_TEST guards in `calculator_core.c` (include block, `sto_pending` visibility, `lvgl_lock` bodies). Bug fixed: `commit_history_entry` self-copy when re-evaluating only history slot (pointer equality guard). Suite 412 → 516 total. **Complexity delta: `neutral`**.
- 2026-04-02: Item 18 display-string replacements — Y= row labels → Y₁–Y₄ (`graph_ui.c:154`); TOKEN_X_INV display-only mappings → ⁻¹ U+E001 (`graph_ui.c`, `ui_prgm.c`). Expression buffer `^-1` kept as intentional deviation (Option B). VARS/Y-VARS strings deferred to when those menus are implemented. **Complexity delta: `neutral`**.
- 2026-04-02: `handle_prgm_menu` split — extracted `handle_erase_confirm`, `enter_exec_tab`, `enter_edit_tab`, `enter_erase_tab` as focused static helpers in `ui_prgm.c`. Dispatcher ~120 → ~50 lines. Zero logic change. `graph_ui.c` pre-existing implicit-fallthrough `-Werror` fixed (`__attribute__((fallthrough))`). **Complexity delta: `neutral`**.
- 2026-04-02: P3 Phase 3 complete — `handle_mode_screen`, `handle_math_menu`, `handle_test_menu`, `handle_matrix_menu` now accept explicit state struct pointers (`ModeScreenState_t *`, `MathMenuState_t *`, `TestMenuState_t *`, `MatrixMenuState_t *`) instead of reading module-level statics. `MatrixMenuState_t` moved from `calculator_core.c` to `ui_matrix.h` (now public extern `matrix_menu_state`); the three former `matrix_tab`/`matrix_item_cursor`/`matrix_return_mode` statics replaced by the consolidated struct. History buffer reduced to TI-81 spec: `HISTORY_LINE_COUNT` 32 → 1, `MATRIX_RING_COUNT` 8 → 1 (~7 KB RAM recovered). Removes the two "Deliberate Deviations" for UP/DOWN multi-step history scroll and `2nd+ENTRY` offset behaviour. Zero logic change — circular buffer math and `handle_history_nav` work correctly at size 1. **Complexity delta: `decrease`** (RAM reduction, state consolidation, handler testability).
- 2026-04-02: P34 parametric graphing complete — `MATH_VAR_T` token + `Calc_PrepareParamEquation`/`Calc_EvalParamEquation` API in `calc_engine`; `GRAPH_NUM_PARAM 3` pairs + `t_min/max/step` + `param_mode` in `GraphState_t`; `PersistBlock_t` v5 (864 → 1264 bytes); `Graph_RenderParametric` + `Graph_InvalidateCache` in `graph.c`; `ui_param_yeq_screen` + `handle_param_yeq_mode` + 9-field parametric RANGE layout in `graph_ui.c`; MODE row 4 handler, `TOKEN_X_T` mode-sensitive insert, `MODE_GRAPH_PARAM_YEQ` dispatch in `calculator_core.c`; 28-assertion `test_param.c` host test suite. 516 → 449 total (prior count was assertions, now tracked per-suite). **Complexity delta: `increase`** (new mode, new screen, tokenizer/evaluator param flags, renderer branch; see P35 complexity item in CLAUDE.md).
- 2026-04-03: P30 STAT menu complete — `StatData_t`/`StatResults_t` + `STAT_MAX_POINTS 99` in `app_common.h`; `MODE_STAT_MENU/EDIT/RESULTS` in `CalcMode_t`; `calc_stat.h`/`calc_stat.c` (1-Var, LinReg, LnReg, ExpReg, PwrReg, SortX, SortY, Clear); `PersistBlock_t` v6 (1264 → 2060 bytes, +stat list 796 B); `ui_stat.h`/`ui_stat.c` (~470 lines, three screens + handlers); `Graph_DrawScatter`/`Graph_DrawXYLine`/`Graph_DrawHistogram` in `graph.c`; `TOKEN_STAT` dispatch + persist + screen init in `calculator_core.c`; 39-assertion `test_stat.c` host test suite; 7 test executables, 488 total assertions. **Complexity delta: `increase`** (new module, 3 modes, 3 screens, 3 graph renderers, +796 B persist; see P30 complexity item in CLAUDE.md).
- 2026-04-04: P35 complexity debt resolved — parametric Y= editor extracted from graph_ui.c into ui_param_yeq.c (325 lines) / ui_param_yeq.h (40 lines); nav_to MODE_GRAPH_PARAM_YEQ block replaced by param_yeq_nav_enter(); graph_ui.c reduced 2026 → 1743 lines (−283). Build clean, all pre-existing tests pass. **Complexity delta: decrease**.
- 2026-04-04: P29 complexity debt (draw layer portion) resolved — draw layer extracted from `graph.c` into `graph_draw.c` (120 lines) / `graph_draw.h` (54 lines); `graph.c` reduced 963 → 881 lines; `apply_draw_layer` replaced by `Graph_ApplyDrawLayer(uint16_t *dest)`; `graph.h` draw-layer section removed; CMakeLists.txt updated. Remaining P29 debt: stat renderers in `graph.c` (tightly coupled, future `graph_stat.c`), `try_execute_draw_command` in `calculator_core.c` (future `ui_draw_exec.c`). Build clean, all pre-existing tests pass. **Complexity delta: `decrease`**.
- 2026-04-04: P29 DRAW menu complete — `MODE_DRAW_MENU` in `CalcMode_t`; `ui_draw.h`/`ui_draw.c` (7-item single-list menu: ClrDraw immediate, Line(/PT-On(/PT-Off(/PT-Chg(/DrawF/Shade( insert to expression buffer); persistent draw layer in SDRAM at 0xD0080800 (320×240 RGB565, 0x0000=transparent sentinel); `Graph_DrawLayerClear/SetPixel/GetPixel/Line/DrawF/Shade` + `apply_draw_layer` + `Graph_IsVisible/MathXToPx/MathYToPx` in `graph.c`; `try_execute_draw_command`/`parse_draw_args`/`eval_draw_arg` + TOKEN_DRAW dispatch in `calculator_core.c`; pre-existing `-Werror` fixes in `graph_ui.c`, `ui_stat.h`, `ui_stat.c`; `ui_draw.h` stubs in `calculator_core_test_stubs.h`. Build clean, 6/7 test suites pass (1 pre-existing param test failure unrelated). Hardware validation pending (P29h). **Complexity delta: `increase`** (new module, draw-layer API, DRAW command parser; see P29 complexity item in CLAUDE.md).
- 2026-04-04: P31 VARS menu complete — `MODE_VARS_MENU` in `CalcMode_t`; `ui_vars.h`/`ui_vars.c` (5-tab menu: XY/Σ/LR/DIM/RNG; inserts current numeric value of selected variable into expression buffer or Y= editor); on-the-fly y-statistics (ȳ, Sy, σy, Σy, Σy², Σxy) from `stat_data`; RNG tab has 10 items with scroll; TOKEN_VARS dispatch + menu_open/menu_close + hide_all_screens in `calculator_core.c`; TOKEN_VARS in `handle_yeq_mode` (graph_ui.c); `ui_vars.c` added to CMakeLists.txt. Build clean, all 7/7 host tests pass. Hardware validation pending. **Complexity delta: `neutral`** (new module same scale as ui_draw.c; no existing file grew).
- 2026-04-04: #6 ZBox render speed — `Graph_DrawZBox` pixel writes changed from per-pixel `lv_canvas_set_px` (O(n) LVGL call chain per pixel + individual `lv_obj_invalidate` per pixel) to direct `graph_buf[]` uint16_t writes + single `lv_obj_invalidate(graph_canvas)` at end. Eliminates ~200+ redundant per-pixel LVGL calls per arrow key event. Build clean, 7/7 tests pass. **Complexity delta: neutral**.
- 2026-04-15: UI super-module Phase 2 extractions — (1) `handle_prgm_running()` moved from `prgm_exec.c` to `ui_prgm.c` (correct home; was an architectural misplacement); (2) `ui_mode.h`/`ui_mode.c` extracted from `calculator_core.c` — MODE screen state, LVGL init, display update, and `handle_mode_screen`; `ui_mode_open()` replaces inline TOKEN_MODE handling; (3) `ui_input.h`/`ui_input.c` extracted — all normal-mode expression input handlers (`expr_insert_str/char/prepend`, `handle_digit_key`, `handle_arithmetic_op`, `handle_function_insert`, `handle_sto_key/pending`, `handle_clear_key`, `handle_normal_graph_nav`, `handle_normal_mode`); `calculator_core.c` reduced 2502 → 2131 lines (−371); `calc_internal.h` updated with new externs and declarations; `calculator_core_test_stubs.h` updated with new stubs; both CMakeLists.txt files updated; pre-existing `test_param` UB fixed (use `√` Unicode glyph instead of unrecognized `"sqrt"` string in error-propagation test). 8/8 host test suites pass. **Complexity delta: `decrease`** (handler code now in focused single-responsibility files; calculator_core.c closer to pure coordinator role).
- 2026-04-15: P32 Y-VARS menu complete — `MODE_YVARS_MENU` in `CalcMode_t`; `TOKEN_Y1–Y4` in `keypad_map.h`; `MATH_VAR_Y1–Y4` + `Calc_RegisterYEquations` + ShuntingYard/ImplicitMulPass handling in `calc_engine.c`; `ui_yvars.h`/`ui_yvars.c` (3-tab menu Y/ON/OFF; Y tab inserts "Y₁"–"Y₄" strings; ON/OFF tabs toggle `graph_state.enabled[]` directly); reentrancy guard prevents infinite recursion in self-referencing Y equations; TOKEN_Y_VARS dispatch + menu_open/menu_close/hide_all_screens in `calculator_core.c`; TOKEN_Y_VARS in `handle_yeq_mode` (graph_ui.c); `ui_yvars.c` added to CMakeLists.txt; stubs in `calculator_core_test_stubs.h`. Build clean, 20/20 host tests pass (test_yvars), all 8/8 test suites pass (1 pre-existing param failure unrelated). Hardware validation pending (P32h). **Complexity delta: neutral** (new module follows established ui_vars.c pattern; no existing file grew significantly).
- 2026-04-15: UI super-module Phase 3 extractions — (1) MATH/NUM/HYP/PRB and TEST menu code extracted from `calculator_core.c` into `ui_math_menu.h`/`ui_math_menu.c` (481 lines) — private types (`MenuItem_t`, `MathMenuState_t`, `TestMenuState_t`), statics, label arrays, data tables, and all handlers; `menu_open`/`menu_close` updated to delegate to new `math_menu_open/close`/`test_menu_open/close` functions; (2) `try_execute_draw_command` + `parse_draw_args` + `eval_draw_arg` moved from `calculator_core.c` into `ui_draw.c` (their natural home — all required headers already present); `try_execute_draw_command` added to `ui_draw.h`; `calculator_core.c` reduced 2131 → 1591 lines (−540); `calc_internal.h` updated; `calculator_core_test_stubs.h` updated with inline stubs (`math_menu_open/close`, `test_menu_open/close` set `current_mode`); `test_normal_mode.c` updated with `ui_math_screen`/`ui_test_screen` definitions; both CMakeLists.txt updated (test build uses stubs — `ui_math_menu.c` excluded); TECHNICAL.md and ARCHITECTURE.md updated. 8/8 host test suites pass, 91/91 check_sync checks pass. **Complexity delta: `decrease`** (−540 lines from coordinator; MATH/TEST menus now in focused single-responsibility file alongside other extracted menus).
- 2026-04-15: graph_ui_range.c extraction — RANGE field editor and ZOOM FACTORS editor screens extracted from `graph_ui.c` into `graph_ui_range.h`/`graph_ui_range.c` (718 lines). Moved: `FieldEditor_t`, `field_editor_handle`, `handle_range_mode`, `handle_zoom_factors_mode`, all RANGE/ZOOM-FACTORS state/LVGL statics, all helpers, persist accessors, `ui_graph_range_screen`/`ui_graph_zoom_factors_screen` definitions. `zoom_menu_reset` made non-static (called from `graph_ui_range.c`); `zoom_enter_factors` updated to call `zoom_factors_nav_enter()`; `nav_to(MODE_GRAPH_RANGE)` updated to call `range_nav_enter()`; `zoom_execute_item` updated to use `graph_ui_get_zoom_x/y_fact()` accessors. `graph_ui.h` updated (includes `graph_ui_range.h`, removes moved declarations, adds `zoom_menu_reset` declaration). graph_ui.c reduced 1743 → 1131 lines (−612). CMakeLists.txt and TECHNICAL.md/ARCHITECTURE.md updated. 8/8 host tests pass, 95/95 check_sync checks pass. **Complexity delta: `decrease`**.
- 2026-04-15: ui_prgm.c sub-menu extraction (INTERFACE_REFACTOR_PLAN Item 1) — CTL, I/O, and EXEC sub-menu handlers extracted from `ui_prgm.c` into three new modules: `ui_prgm_ctl.h`/`ui_prgm_ctl.c`, `ui_prgm_io.h`/`ui_prgm_io.c`, `ui_prgm_exec.h`/`ui_prgm_exec.c`. Each follows the `ui_math_menu.h` pattern: non-static screen pointer extern in header, `ui_init_*_screen(parent)` + `ui_prgm_*_reset_and_show()` + `ui_update_*_display()` + `handle_prgm_*_menu()` API. Shared helpers (`prgm_editor_insert_str`, `prgm_flatten_to_store`, `prgm_submenu_return_to_editor`, `prgm_submenu_tab_switch`) made non-static and declared in `ui_prgm.h`. `ui_prgm.c` reduced 1652 → 1276 lines. TECHNICAL.md/ARCHITECTURE.md/CLAUDE.md updated. 8/8 host test suites pass. **Complexity delta: `neutral`** (pure extraction; no new logic).
- 2026-04-16: Firmware build fixes — three pre-existing compilation errors from Item 1: (1) `ui_mode.c` missing `#include "ui_palette.h"` (COLOR_* undeclared); (2) `ui_math_menu.h` missing `#include "lvgl.h"` (lv_obj_t unknown type); (3) `ui_math_menu.c` missing `#include <stdio.h>` (snprintf implicit); (4) firmware CMakeLists.txt missing `expr_buffer.c`, `ui_prgm_ctl.c`, `ui_prgm_io.c`, `ui_prgm_exec.c` (linker errors); (5) `handle_prgm_running` in `ui_prgm.c` accessing `static` variables from `prgm_exec.c` directly after move — replaced with accessor API (`prgm_is_waiting_input`, `prgm_get_input_var`, `prgm_clear_input_wait`, `prgm_reset_execution_state`). Firmware builds 0 errors. 9/9 host tests pass.
- 2026-04-16: ExprBuffer_t wrapper (INTERFACE_REFACTOR_PLAN Item 2) — `MAX_EXPR_LEN` moved to `app_common.h`; `ExprBuffer_t` struct (buf/len/cursor) + 5 accessor API added to `expr_util.h`; `expr_buffer.c` new file (zero LVGL/HAL dependencies); `calculator_core.c` three raw globals replaced by `ExprBuffer_t expr`; `calc_internal.h` updated to single extern; `ui_input.c`, `ui_prgm.c`, `ui_draw.c`, `prgm_exec.c` migrated to `expr.buf/len/cursor`; test stubs updated; `test_expr_buffer.c` added (8 groups / 48 assertions); `expr_util.c` off-by-one bug fixed in `ExprUtil_InsertChar` overflow guard (`> max_len` → `>= max_len`). 9/9 host test suites pass, 105/105 check_sync checks pass. **Complexity delta: `neutral`** (pure structural wrapping; no new logic).
- 2026-04-16: MenuState_t navigation helpers (INTERFACE_REFACTOR_PLAN Item 3) — `MenuState_t` struct + 5 helpers (`MoveUp`, `MoveDown`, `PrevTab`, `NextTab`, `DigitToIndex`) + `AbsoluteIndex` inline in new `menu_state.h`/`menu_state.c` (zero LVGL/HAL dependencies); `VarsMenuState_t` bespoke struct removed from `ui_vars.h`; `ui_vars.c` migrated to `MenuState_t` with UP/DOWN using `MenuState_MoveUp/Down`, digit shortcuts using `MenuState_DigitToIndex`; `calculator_core.c` field names updated (`item_cursor` → `cursor`, `scroll_offset` → `scroll`); test stubs (`calculator_core_test_stubs.h`, `test_normal_mode.c`) updated; `test_menu_state.c` added (5 groups / 43 assertions); TODO migration notes added to `ui_stat.c`, `ui_math_menu.c`, `ui_matrix.c`, `ui_yvars.c`, `ui_draw.c`. 10/10 host test suites pass, 694 total assertions, 108/108 check_sync checks pass. **Complexity delta: `neutral`** (pure structural unification; no logic changes).
- 2026-04-16: ZOOM menu extraction (INTERFACE_REFACTOR_PLAN Item 4) — `ui_graph_zoom.h`/`ui_graph_zoom.c` new files; 10 functions extracted from `graph_ui.c` (`ui_init_zoom_screen`, `apply_zoom_preset`, `ui_update_zoom_display`, `zoom_menu_reset`, `zoom_show_graph`, `zoom_enter_factors`, `zoom_scale_view`, `zoom_execute_item`, `handle_zoom_mode`); `graph_ui.c` 1131 → 862 lines; `ui_update_zoom_display` / `handle_zoom_mode` removed from `graph_ui.h`; `calculator_core.c` includes `ui_graph_zoom.h`. `zoom_enter_zbox` kept in `graph_ui.c` — owns `s_zbox` state. **Complexity delta: `neutral`**.
- 2026-04-16: graph_state ownership documentation (INTERFACE_REFACTOR_PLAN Item 5) — ownership comment block added above `GraphState_t` in `app_common.h`; "State Ownership" section added to `docs/TECHNICAL.md` covering `graph_state`, `calc_variables[]`, `calc_matrices[]`, `stat_data`, and `ans`. No code changes. **Complexity delta: `neutral`**.
- 2026-04-16: PersistBlock_t sub-struct split design (INTERFACE_REFACTOR_PLAN Item 6) — "Persist Migration Design" section added to `docs/TECHNICAL.md`: current flat layout problem, proposed sub-struct layout (`GraphPersist_t`, `StatPersist_t`, `MatrixPersist_t`, `PrgmPersist_t`, `ModePersist_t`), field assignment table, per-section migration switch pseudocode, adoption constraint (atomic at next version bump), and implementation order. No code changes. All 6 INTERFACE_REFACTOR_PLAN items now complete. **Complexity delta: `neutral`**.
- 2026-04-16: COUPLING_REFACTOR T2 (`g_prgm_store` accessor API) — 7 accessor functions added to `prgm_exec.h`/`prgm_exec.c` (`Prgm_GetName`, `Prgm_GetBody`, `Prgm_IsSlotOccupied`, `Prgm_SetName`, `Prgm_AppendLine`, `Prgm_SetBody`, `Prgm_ClearSlot`); all direct `g_prgm_store` field writes migrated in `ui_prgm.c` (12 sites) and `ui_prgm_exec.c` (2 sites); both test stubs (`prgm_exec_test_stubs.h`, `calculator_core_test_stubs.h`) updated; `test_prgm_exec.c` and `test_normal_mode.c` reset helpers migrated; `extern ProgramStore_t g_prgm_store` removed from `prgm_exec.h`; `g_prgm_store` is now an exclusively-internal variable in `prgm_exec.c` (embedded) / test TU (HOST_TEST). 10/10 ctest pass, firmware builds 0 errors, check_sync 111/111. **Complexity delta: `neutral`** (7 thin wrappers; no new control flow).
- 2026-04-16: COUPLING_REFACTOR T3 (ANS getter/setter) — `calculator_core.h` new public header; 4 functions added (`Calc_GetAns`, `Calc_GetAnsIsMatrix`, `Calc_SetAnsScalar`, `Calc_SetAnsMatrix`); `ans` and `ans_is_matrix` made static in `calculator_core.c` (conditional on `#ifndef HOST_TEST`); `format_calc_result` signature simplified (removed `float *ans_out` out-parameter — function now writes `ans` directly as the owning TU); 6 migration sites across `ui_input.c`, `ui_prgm.c`, `ui_draw.c`, `prgm_exec.c` (Calc_Evaluate read sites + write sites); `extern float ans` / `extern bool ans_is_matrix` removed from `calc_internal.h`; `prgm_exec_test_stubs.h` updated with inline ANS stubs; `calculator_core_test_stubs.h` updated with 4 function declarations; `TECHNICAL.md` ans/ans_is_matrix ownership section updated. Direct field writes outside `calculator_core.c` are now compile errors. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `neutral`** (4 one-liner functions; signature simplification removes one out-pointer; call sites net no new lines).
- 2026-04-16: COUPLING_REFACTOR T1 (graph state accessor layer) — `graph_state` moved from `calculator_core.c` to `graph.c` (now `static`); 13 accessor functions added (`Graph_GetState`, `Graph_SetWindow`, `Graph_SetEquationEnabled`, `Graph_SetParamEnabled`, `Graph_SetParamMode`, `Graph_SetParamWindow`, `Graph_SetGridOn`, `Graph_SetActive`, `Graph_GetEquationBuf`, `Graph_GetParamEquationXBuf`, `Graph_GetParamEquationYBuf`); `GRAPH_EQUATION_BUF_LEN` constant added; 9 modules migrated away from direct extern field writes (`graph_ui.c`, `graph_ui_range.c`, `ui_param_yeq.c`, `ui_mode.c`, `ui_yvars.c`, `ui_graph_zoom.c`, `ui_vars.c`, `ui_input.c`, `calculator_core.c`); `extern GraphState_t graph_state` removed from `app_common.h`; `ui_graph_zoom.c` and `menu_state.c` added to firmware CMakeLists.txt (were missing); `#include "graph.h"` added to `ui_vars.c`, `ui_param_yeq.c`, `graph_ui_range.c`; host-test stubs updated with `GRAPH_EQUATION_BUF_LEN`. Direct field writes outside `graph.c` are now compile errors. API/header design A → A+. 10/10 ctest pass, firmware builds 0 errors, check_sync 111/111. **Complexity delta: `neutral`**.
- 2026-04-17: COUPLING_REFACTOR T7 (`Execute_Token` dispatch table) — `ModeHandler_t` function-pointer typedef + `ModeEntry_t` struct typedef added; 3 thin wrappers (`dispatch_matrix_menu`, `dispatch_matrix_edit`, `dispatch_stat_menu`) capture non-uniform signatures; `k_mode_handlers[]` static const table with 22 entries replaces 22-entry sequential if-chain; `_Static_assert(ARRAY_SIZE(k_mode_handlers) == 22, ...)` guards table integrity at compile time; ALPHA_LOCK compound cases (`MODE_PRGM_NEW_NAME`, `MODE_PRGM_EDITOR`) kept as explicit checks outside the table; `MODE_PRGM_RUNNING` always-return case kept before the table. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `decrease`** (Execute_Token dispatcher reduced from 24 branches to 5; table is pure declarative data).
- 2026-04-17: COUPLING_REFACTOR T6 (`menu_open`/`menu_close` module delegation) — `Matrix_MenuOpen/Close`, `Stat_MenuOpen/Close`, `Draw_MenuOpen/Close`, `Vars_MenuOpen/Close`, `Yvars_MenuOpen/Close` added to their owning modules (10 one-liner functions each); inline state-init blocks removed from `menu_open()`/`menu_close()` in `calculator_core.c`; both dispatcher switches now consist entirely of single-line delegation calls; corresponding inline stubs added to `calculator_core_test_stubs.h`; `calculator_core.c` reduced 1591 → 1544 lines. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `decrease`** (ten multi-line blocks → ten one-liners; logic moved to owning modules).
- 2026-04-17: COUPLING_REFACTOR T5 (Mode transition via `Calc_SetMode()`) — 4 accessor functions added to `calculator_core.h`/`calculator_core.c` (`Calc_SetMode`, `Calc_SetReturnMode`, `Calc_GetMode`, `Calc_GetReturnMode`); `current_mode` and `return_mode` made `static` in `calculator_core.c`; `extern CalcMode_t current_mode/return_mode` removed from `calc_internal.h`; ~20 direct write/read sites migrated across 13 files (`ui_mode.c`, `ui_math_menu.c`, `ui_matrix.c`, `ui_stat.c`, `ui_draw.c`, `ui_vars.c`, `ui_yvars.c`, `ui_prgm.c`, `graph_ui.c`, `graph_ui_range.c`, `ui_graph_zoom.c`, `ui_param_yeq.c`, `prgm_exec.c`); CLAUDE.md gotcha #21 MODE_STO pattern and gotcha #12 TRACE fallthrough preserved unchanged; `ui_draw.c` and `ui_vars.c` had only struct-field `return_mode` accesses — no changes needed. Direct `current_mode`/`return_mode` assignments outside `calculator_core.c` are now compile errors. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `neutral`** (4 one-liner functions; pure mechanical substitution).
- 2026-04-17: COUPLING_REFACTOR T4 (LVGL screen show/hide API) — 16 screen pointers made `static` across their owning modules; extern screen pointer block removed from `calc_internal.h`; extern declarations removed from `graph_ui.h`, `graph_ui_range.h`, `ui_graph_zoom.h`, `ui_param_yeq.h`, `ui_matrix.h`, `ui_stat.h`, `ui_draw.h`, `ui_vars.h`, `ui_yvars.h`, `ui_math_menu.h`, `ui_mode.h`; show/hide/IsVisible one-liner functions added to all 11 owning modules (`Graph_ShowYeqScreen/HideYeqScreen/IsYeqScreenVisible`, `Graph_ShowRangeScreen/HideRangeScreen/IsRangeScreenVisible`, `Graph_ShowZoomFactorsScreen/HideZoomFactorsScreen/IsZoomFactorsScreenVisible`, `Zoom_ShowScreen/HideScreen`, `ParamYeq_ShowScreen/HideScreen`, `Matrix_ShowMenuScreen/HideMenuScreen/ShowEditScreen/HideEditScreen/IsEditScreenVisible`, `Stat_ShowMenuScreen/HideMenuScreen/ShowEditScreen/HideEditScreen/ShowResultsScreen/HideResultsScreen`, `Draw_ShowScreen/HideScreen`, `Vars_ShowScreen/HideScreen`, `Yvars_ShowScreen/HideScreen`, `Math_ShowScreen/HideScreen`, `Test_ShowScreen/HideScreen`, `Mode_HideScreen`); cross-module zoom-screen refs in `graph_ui.c` and `graph_ui_range.c` replaced with `Zoom_ShowScreen/HideScreen`; `hide_all_screens()`, `menu_open()`, `menu_close()`, `cursor_timer_cb()`, `ui_update_status_bar()` in `calculator_core.c` fully migrated; all new stubs added to `calculator_core_test_stubs.h`. Direct `lv_obj_*` calls on other modules' screen pointers are now compile errors in `calculator_core.c`. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `neutral`** (one-liner wrappers; no new logic or data structures).
- 2026-04-17: COUPLING_REFACTOR T8 (Extract `calc_history.c`) — history ring buffer state (`s_history[]`, `s_history_count`, `s_history_recall_offset`, `s_matrix_scroll_focus`, `s_matrix_scroll_offset`) moved from `calculator_core.c` statics into new `App/Src/calc_history.c` / `App/Inc/calc_history.h`; 13-function `CalcHistory_*` API (`Init`, `Commit`, `Clear`, `RecallUp`, `RecallDown`, `GetCount`, `GetRecallOffset`, `ResetRecallOffset`, `GetEntry`, `GetExprForRecall`, `ResetMatrixScroll`, `GetMatrixScrollFocus`, `GetMatrixScrollOffset`, `SetMatrixScrollOffset`); `CalcHistory_UpdateDisplay()` declared in `calc_history.h` but defined in `calculator_core.c` (needs private `disp_rows[]`); extern history state removed from `calc_internal.h` (replaced by `#include "calc_history.h"`); `HISTORY_LINE_COUNT`, `MAX_RESULT_LEN`, `MATRIX_RING_COUNT`, `HistoryEntry_t` moved to `calc_history.h`; 6 call sites migrated (`calculator_core.c`, `ui_input.c`, `prgm_exec.c` ×3, `ui_prgm.c`); test infra: `calc_history.c` added to `test_normal_mode` target; `prgm_exec_test_stubs.h` updated with `CalcHistory_*` + `Calc_SetMode`/`Calc_GetMode` inline stubs; `calculator_core_test_stubs.h` updated (stale `ui_mode_screen` extern removed; `current_mode`/`return_mode` made non-static in HOST_TEST builds to match `ans` pattern); `TECHNICAL.md` + `ARCHITECTURE.md` updated with `calc_history.c/h`. 10/10 ctest pass (694 assertions), firmware builds 0 errors, check_sync 114/114. **Complexity delta: `neutral`** (~150 lines moved; no new control flow).
- 2026-04-17: COUPLING_REFACTOR T10 (first module — `ui_yvars.c`) — `YVarsMenuState_t` bespoke struct (tab/item_cursor/return_mode) removed from `ui_yvars.h`; `ui_yvars.c` migrated to `MenuState_t` from `menu_state.h`; `item_cursor` field renamed to `cursor`; manual UP/DOWN bounds-check logic replaced with `MenuState_MoveUp/Down(s, total, MENU_VISIBLE_ROWS)`; `tab_move` calls updated (`&s->item_cursor` → `&s->cursor`); `Yvars_MenuOpen/Close` updated to initialise `cursor` and `scroll` explicitly; `YVarsMenuState_t` typedef removed from `calculator_core_test_stubs.h`; definition in `test_normal_mode.c` updated to `MenuState_t yvars_menu_state = {0}`. Four remaining modules (`ui_stat.c`, `ui_math_menu.c`, `ui_matrix.c`, `ui_draw.c`) retain their bespoke types with TODO notes. 10/10 ctest pass (694 assertions), firmware builds 0 errors, check_sync 115/115. **Complexity delta: `decrease`** (bespoke typedef removed; duplicate UP/DOWN bounds-check code eliminated; no new logic).
- 2026-04-17: COUPLING_REFACTOR T10 (modules 2–5: `ui_math_menu.c`, `ui_matrix.c`, `ui_stat.c`, `ui_draw.c`) — `MathMenuState_t`, `TestMenuState_t`, `MatrixMenuState_t`, `StatMenuState_t`, `DrawMenuState_t` bespoke typedefs removed; all 5 modules migrated to `MenuState_t`; `item_cursor` → `cursor` throughout; `scroll_offset` → `scroll` (math menu); manual UP/DOWN bounds-check blocks replaced with `MenuState_MoveUp/Down(s, count, MENU_VISIBLE_ROWS)`; `tab_move` calls updated to pass `&s->cursor` and `&s->scroll`; public typedefs removed from headers and replaced with `#include "menu_state.h"`; `MatrixMenuState_t`, `StatMenuState_t`, `DrawMenuState_t` typedef blocks removed from `calculator_core_test_stubs.h`; `test_normal_mode.c` definitions updated to `MenuState_t {0}`. All 5 modules committed individually (3/5, 4/5, 5/5). 10/10 ctest pass (694 assertions), firmware builds 0 errors, check_sync 115/115 after each. **Complexity delta: `neutral`** (equivalent logic; bespoke types eliminated; no new control flow). COUPLING_REFACTOR complete — all 10 tasks done; `COUPLING_REFACTOR.md` deleted.
- 2026-04-17: COUPLING_REFACTOR T9 (Move `BuildPersistBlock`/`ApplyPersistBlock` to `persist.c`) — `Calc_BuildPersistBlock` and `Calc_ApplyPersistBlock` (~113 lines) removed from `calculator_core.c` and reimplemented as `Persist_BuildBlock(void) → PersistBlock_t` and `Persist_ApplyBlock(const PersistBlock_t *)` in `persist.c`; `persist.c` gains 5 new includes inside `#ifndef HOST_TEST` (`calc_engine.h`, `calculator_core.h`, `ui_mode.h`, `graph.h`, `graph_ui_range.h`); `Persist_BuildBlock` return-by-value replaces output-pointer pattern; `Calc_GetAngleDegrees`/`Calc_SetAngleDegrees` added to `calculator_core.h`/`calculator_core.c` (2 one-liners) so `persist.c` can restore angle mode without including `calc_internal.h`; old `Calc_*` names removed from `persist.h` and `persist.c`; call site in `Execute_Token` (save path) and `StartCalcCoreTask` (restore path) updated; `calculator_core_test_stubs.h` updated with `Persist_BuildBlock`/`Persist_ApplyBlock` declarations; `test_normal_mode.c` gains two no-op stub definitions; `ui_mode.h` comment updated; `docs/TECHNICAL.md` and `docs/POWER_MANAGEMENT.md` updated; `calculator_core.c` reduced 1544 → 1453 lines; `persist.c` grew 146 → 281 lines. 10/10 ctest pass (694 assertions), firmware builds 0 errors, check_sync 115/115. **Complexity delta: `neutral`** (~113 lines relocated; no new control flow; 2 new one-liner accessors).
- 2026-04-17: REVIEW Item 7 (graph.c three functions over 80 lines) — `graph_draw_trace_param` and `graph_draw_trace_func` extracted from `Graph_DrawTrace()` (118 → 17 lines); `graph_render_setup()` extracted as a shared static helper called from both `Graph_Render()` and `Graph_RenderParametric()` (eliminates 4-line fill_bg/grid/axes/ticks duplication); `GRAPH_BUF_ADDR 0xD0025800UL` named constant replaces two bare hex literals. Firmware builds 0 errors, 10/10 ctest pass. **Complexity delta: `decrease`** (longest function in module 118 → 17 lines; setup duplication eliminated).
- 2026-04-18: REVIEW Items 5 & 6 verified complete — Item 5 (`Process_Hardware_Key` accessor discipline) was already applied as part of COUPLING_REFACTOR T5; Item 6 (y-stat functions in `calc_stat.c`, `ui_vars.c` migration, `test_stat.c` assertions) fully implemented. `REVIEW_FOLLOWUP_2026-04-17.md` deleted — all 8 items resolved. **Complexity delta: `neutral`**.
- 2026-04-18: REVIEW Item 1 (cursor overlay dispatch duplication) — `Prgm_IsEditorScreenVisible()` and `Prgm_IsNewScreenVisible()` added to `ui_prgm.c`/`ui_prgm.h`; raw `extern lv_obj_t *ui_prgm_editor_screen/new_screen` removed from `ui_prgm.h`; duplicate 6-branch cursor dispatch chains in `cursor_timer_cb` and `ui_update_status_bar` extracted into `update_overlay_cursor()` static helper in `calculator_core.c`; test stubs (`calculator_core_test_stubs.h`) and stub definitions (`test_normal_mode.c`) cleaned up. 10/10 ctest pass. **Complexity delta: `neutral`** (refactor only; no new logic).
- 2026-04-18: REVIEW Item 8 (`eval_matrix_func` branch extraction) — 8 if-branches each pulled into a named static helper (`eval_mat_transpose`, `eval_mat_det`, `eval_mat_rowswap`, `eval_mat_rowplus`, `eval_mat_mrow`, `eval_mat_mrowplus`, `eval_mat_round`, `eval_mat_arith`); `eval_matrix_func` reduced to an 8-line dispatch; `MAT_ERR` file-scoped macro replaces per-function `MERR`; zero behaviour change; all 10 host test suites pass. **Complexity delta: `decrease`** (123-line function → 8-line dispatch; complexity debt ceiling still 2 open items).
- 2026-04-24: ZOOM Trig default constants corrected — `Ymin`/`Ymax` changed from ±4 to ±3, `Yscl` from 1.0 to 0.25, matching guidebook p. 3-15. One constant in `ui_graph_zoom.c` line 205. **Complexity delta: `neutral`**.
- 2026-04-24: EE glyph deviation documented + MATRX TI-82 extras removed — `TOKEN_EE` `*10^` deviation added to "Deliberate Deviations" table in `CLAUDE.md`; `dim(`, `fill(`, `identity(`, `randM(` removed from MATRX MATH tab (`ui_matrix.c`); tab item count 6→2; MATRIX partially-implemented note updated. **Complexity delta: `decrease`** (4 menu entries removed; no new code).
- 2026-04-24: Free-roaming crosshair + TRACE toggle fix — `MODE_GRAPH_FREE_CURSOR` added to `CalcMode_t`; `Graph_DrawFreeCursor`/`Graph_EraseFreeCursor` added to `graph.c`/`graph.h`; `FreeCursorState_t` + `free_cursor_blink_cb` (LVGL 500 ms timer) + `free_cursor_stop` + `handle_free_cursor_mode` added to `graph_ui.c`; `handle_free_cursor_mode` registered in `k_mode_handlers[]` (count 22→23); all `nav_to(MODE_NORMAL)` graph-entry call sites updated to `nav_to(MODE_GRAPH_FREE_CURSOR)` across 7 files (`graph_ui.c`, `graph_ui_range.c`, `ui_graph_zoom.c`, `ui_param_yeq.c`, `ui_input.c`, `ui_matrix.c`, `ui_math_menu.c`); ZBOX default fall-through kept as `nav_to(MODE_NORMAL)`; `handle_trace_mode` `TOKEN_TRACE` changed from toggle-exit to re-snap to viewport centre; `TOKEN_GRAPH` in trace handler changed to `nav_to(MODE_GRAPH_FREE_CURSOR)`; hardware validation item added to CLAUDE.md. **Complexity delta: `increase`** (new mode, new state struct, new LVGL timer, new handler; see complexity follow-up item in CLAUDE.md).
- 2026-04-24: P33 Normal/Sci/Eng notation closed out — feature was implemented in commit `23be3e2` (2026-04-23) but never removed from CLAUDE.md; host test stub fixed (missing `handle_free_cursor_mode` in `calculator_core_test_stubs.h`); stale test assertion corrected in `test_normal_mode.c` (TOKEN_GRAPH → MODE_GRAPH_FREE_CURSOR); MENU_SPECS.md Row 1 marked wired; CLAUDE.md P33 item removed. All 10/10 suites pass, 742 assertions.
- 2026-04-24: [complexity] P30 DATA editor extracted — `ui_stat_edit.c` (387 lines) split out from `ui_stat.c` (703→367 lines); `Stat_EditOpen()` replaces inline DATA→Edit block in `handle_stat_menu`; `stat_edit_commit` migrated from direct `stat_data` field access to `Stat_GetData()`/`Stat_SetData()` copy-modify-write (consistent with T11 accessor discipline); dead `extern menu_insert_text` declaration removed; `ui_stat_edit.c` registered in `CMakeLists.txt` and `TECHNICAL.md`; `MAINTENANCE_STANDARDS.md` not updated (Code organisation still B — both `ui_stat.c` 367 L and `ui_stat_edit.c` 387 L are under 500-line threshold). 10/10 ctest pass, firmware builds 0 errors, check_sync 116/116. **Complexity delta: `decrease`** (monolithic file split; no new logic).
- 2026-04-24: MATRX row operations in menu — `rowSwap(`, `row+(`, `*row(`, `*row+(` added to MATRX MATH tab as items 1–4; `det(` shifted to 5, `T` shifted to 6; `matrix_tab_item_count[0]` 2→6; digit shortcut range `TOKEN_1...TOKEN_6` already covered 6 items. Build clean. **Complexity delta: `neutral`**.
- 2026-04-24: STAT_MAX_POINTS raised 99→150 — matches guidebook p. 7-2 "up to 150 data points"; FLASH budget confirmed (2472 B << 131072 B sector 10); `PERSIST_VERSION` 7→8 (struct layout change); test_persist_roundtrip size assertion 2060→2472; stack allocations in `calc_stat.c` (two `float[150]` arrays = 1200 B) remain within calcCore 8 KB stack. **Complexity delta: `neutral`**.
- 2026-04-25: Hyperbolic functions engine — 6 `MathTokenType_t` enum values added (`MATH_FUNC_SINH/COSH/TANH/ASINH/ACOSH/ATANH`); 6 keyword-table entries added to `funcs[]` (hyperbolic names placed before their shorter `sin`/`cos`/`tan`/`asin`/`acos`/`atan` prefixes); `is_function()` updated; 6 `eval_unary_func()` cases backed by `<math.h>` `sinhf/coshf/tanhf/asinhf/acoshf/atanhf` (domain errors for `acosh(x<1)` and `atanh(|x|≥1)`); 16 new host assertions in `test_calc_engine.c` group 5b (round-trip identities, domain errors, prefix-ordering regression). 10/10 suites pass, 771 assertions total. **Complexity delta: `neutral`** (6 new enum values, 6 keyword entries, 6 eval cases; no new data structures, files, or control-flow branches).
- 2026-04-25: [bug] `End` subroutine return — `cmd_end()` in `prgm_exec.c` changed from no-op to `prgm_run_pc = prgm_run_num_lines`; exhausting the line counter causes `prgm_run_loop()` to pop the call frame on a subroutine return or finalize execution at the top level, reusing existing implicit-return machinery without duplicating it; 12 new host assertions in `test_prgm_exec.c` Group 18 (End at top-level stops program, End mid-subroutine returns to caller, nested End returns one level). 10/10 suites pass, 787 assertions total. **Complexity delta: `neutral`** (1 line changed, no new data structures or files).
- 2026-04-25: `{x}(n)` / `{y}(n)` list-element access — `MATH_FUNC_LIST_X`/`MATH_FUNC_LIST_Y` added to `MathTokenType_t`; `{x}`/`{y}` entries added to `funcs[]` keyword table; `is_function()` updated; `Calc_RegisterStatAccessors(get_x, get_y, get_len)` registration pattern introduced (decouples `calc_engine.c` from UI layer — parallels `Calc_RegisterYEquations`); 3 static accessor helpers + registration call added to `calculator_core.c` in non-HOST_TEST block; `TOKEN_LIST_X`/`TOKEN_LIST_Y` wired to insert `{x}(`/`{y}(` in `handle_function_insert()` and added to dispatch group in `handle_normal_mode()`; `eval_unary_func()` cases implement 1-based indexing into registered stat list with `CALC_ERR_DOMAIN` on out-of-range or unregistered accessors; 13 new host assertions in `test_calc_engine.c` group 5d (valid reads, compound expressions, bounds errors, unregistered case). 10/10 suites pass, 831 assertions total. **Complexity delta: `neutral`** (2 enum values, 1 registration fn, 2 keyword entries, 2 `is_function` entries, 2 eval cases, 2 insert cases, 3 accessors; no new files).
- 2026-04-25: `nDeriv(` numerical derivative — `MATH_FUNC_NDERIV` token added to `MathTokenType_t`; `is_function()` updated; special-case handler in `try_tokenize_identifier()` extracts arg1 as a string, compiles it into static `s_nderiv_eq` via `Calc_PrepareGraphEquation`, emits `MATH_FUNC_NDERIV + MATH_PAREN_LEFT` and advances past arg1+comma so args 2 (X var) and 3 (val) tokenise normally; `MATH_FUNC_NDERIV` evaluator block in `EvaluateRPN()` pops val+X-ref, evaluates `s_nderiv_eq` at val±ε=0.001 via `Calc_EvalGraphEquation`, pushes `(f⁺−f⁻)/(2ε)`; 6 host assertions in new group 5c (`test_calc_engine.c`); `NEAR_D` 5×10⁻⁴ tolerance macro accounts for float32 catastrophic cancellation in polynomial derivatives at non-zero points. 10/10 suites pass, 793 assertions total. **Complexity delta: `neutral`** (1 enum value, 1 static, 1 `is_function` entry, ~35-line tokeniser hook, ~15-line evaluator case; no new files).
- 2026-04-25: Stat data point INS/DEL — `CalcStat_InsertRow(d, at_row)` and `CalcStat_DeleteRow(d, row)` added to `calc_stat.c`/`calc_stat.h` (pure data manipulation, shift logic, capacity guard); `stat_edit_col=2` row-select state added to `ui_stat_edit.c`: LEFT from col=0 enters col=2 (`>` prefix on cursor row), LEFT from col=2 moves to previous row, RIGHT from col=2 returns to col=0; TOKEN_INS at col=2 calls `CalcStat_InsertRow`, positions cursor at new row col=0; TOKEN_DEL at col=2 calls `CalcStat_DeleteRow`, clamps cursor; digit/decimal/neg at col=2 silently transitions to col=0 with fresh buffer; display edit-buffer override guards col=2 case to prevent ybuf corruption; TOKEN_CLEAR at col=2 exits to STAT menu; 37 new host assertions in `test_stat.c` (`test_insert_row` / `test_delete_row`). 10/10 suites pass, 830 assertions total. **Complexity delta: `neutral`** (no new files; helpers moved to existing pure-math layer; col state +1 value).
- 2026-04-25: [bug] Auto-close trailing `(` — `sy_drain_stack()` in `calc_engine.c` now discards unmatched `(` as implicit `)` per guidebook p. 1-9, and emits any pending function token immediately below it (mirrors the `)` handler exactly); `CALC_ERR_SYNTAX` return removed from that branch; `test_calc_engine.c` updated: `(2+2` test flipped from SYNTAX to `CALC_OK+4.0f`; 4 new assertions (`sin(0`, `√(4`, `abs(-3`, `(1+(2+3`); `test_param.c` invalid-expression test changed from `(+` (now passes SY) to `)1+2` (unmatched close still errors). 10/10 suites pass, 775 assertions total. **Complexity delta: `neutral`** (1 error-return replaced by 4-line implicit-close block; no new files).
- 2026-04-25: Parametric Y-VARS tabs — Y tab expanded to 10 items (Y₁–Y₄, X₁t, Y₁t, X₂t, Y₂t, X₃t, Y₃t); ON/OFF tabs expanded to 8 items each (All + Y₁–Y₄ + X₁t–X₃t); scroll indicators added; `yvars_do_enable` handles parametric pairs via `Graph_SetParamEnabled`; ENTER uses `MenuState_AbsoluteIndex`; digit shortcuts TOKEN_6–TOKEN_0. 10/10 suites, 831 assertions. **Complexity delta: `neutral`** (parallel to VARS/ZOOM scrolling patterns; no new architecture).
- 2026-04-30: T2-B arch review — `angle_degrees` parameter removed from `Graph_Render`, `Graph_DrawTrace`, `Graph_DrawFreeCursor`, `Graph_DrawZBox` signatures; all 17 call sites in `graph_ui.c` and 8 in `prgm_exec.c` updated; functions now call `Calc_GetAngleDegrees()` internally (already present in `graph.c`); `extern bool angle_degrees` include path removed from `graph_ui.c`. 11/11 host suites pass, 847 assertions. **Complexity delta: `decrease`** (17 parameter-threading sites eliminated; graph.h surface cleaner).
- 2026-04-30: T2-C arch review — graph cursor state moved from `graph_ui.c` into `graph.c`; `TraceState_t`, `FreeCursorState_t` (without blink timer), `ZBoxState_t` type definitions moved to `graph.h`; 3 state instances (`s_trace`, `s_free`, `s_zbox`) moved to `graph.c` as statics; 12 accessor functions added (`Graph_GetTraceState`, `Graph_SetTraceX`, `Graph_SetTraceEqIdx`, `Graph_GetFreeCursorState`, `Graph_SetFreeCursorPos`, `Graph_SetFreeCursorVisible`, `Graph_GetZBoxState`, `Graph_SetZBoxCursorPos`, `Graph_SetZBoxCorner1`, `Graph_ClearZBoxCorner1`, `Graph_ResetZBox`); `s_free.blink_timer` kept as local `s_free_blink_timer` static in `graph_ui.c`; `zoom_enter_zbox()` calls `Graph_ResetZBox()` instead of direct field writes; all `handle_trace_mode`, `handle_zbox_mode`, `handle_free_cursor_mode`, `free_cursor_blink_cb`, `free_cursor_stop`, `nav_to` call sites migrated to use const getter + named setter pattern. 11/11 host suites pass, 847 assertions. **Complexity delta: `neutral`** (state and interfaces relocated; no new logic).
- 2026-04-30: T3-B arch review — `Calc_Parse` + `Calc_Eval` split added to `calc_engine.h`/`calc_engine.c`; `ParsedExpr_t` type (two `GraphEquation_t` fields: `postfix` + `nested`) stores shunted postfix and nDeriv inner expression together so callers hold one struct; `EvaluateRPN` renamed to `EvaluateRPN_ex` with `nderiv_eq` parameter; thin `EvaluateRPN` wrapper preserves old callers via `&s_nderiv_eq`; `Tokenize` and `try_tokenize_identifier` gain optional `GraphEquation_t *nderiv_out` parameter — old callers pass `NULL` (writes to `s_nderiv_eq` as before), `Calc_Parse` passes `&out->nested` so nDeriv inner expression lives in the `ParsedExpr_t` rather than shared static; `graph.c` upgrades `eq_postfix[4]` and `param_postfix_x/y[2]` from `GraphEquation_t` to `ParsedExpr_t`; all 7 `Calc_PrepareGraphEquation`/`Calc_PrepareParamEquation` and 7 `Calc_EvalGraphEquation`/`Calc_EvalParamEquation` call sites in `graph.c` converted to `Calc_Parse`/`Calc_Eval`; old Prepare/Eval functions retained for backward compat; `test_parse_eval.c` added (12 suites, 103 assertions: basic equivalence, reuse, parametric, nDeriv value, two-ParsedExpr_t nDeriv independence, parse-error propagation, PrepareGraphEquation equivalence); `CMakeLists.txt` updated. 12/12 suites pass, 950 assertions. **Complexity delta: `decrease`** (nDeriv static side-effect made explicit; old shared-static s_nderiv_eq retained for legacy path, removed in follow-up).
- 2026-05-01: F2 arch review — CalcMode_t topology enforcement added. `MODE_COUNT` sentinel added to `CalcMode_t` in `app_common.h`; `calc_mode_topology_validate()` added to `calculator_core.c` (HOST_TEST only): walks [0, MODE_COUNT) and asserts each value appears in exactly one of `k_route_table[]` (non-fallback pred fires when `current_mode == mode`, neutral token TOKEN_ENTER, `return_mode`/`sto_pending` at defaults) or `known_special_cases[]` (MODE_NORMAL, MODE_2ND, MODE_ALPHA, MODE_ALPHA_LOCK, MODE_STO); also verifies no stale out-of-range entries; function declared in `calculator_core.h` under `#ifdef HOST_TEST`; `test_mode_topology.c` added (1 assertion); `CMakeLists.txt` updated (new target + `mode_topology` ctest entry). Adding a new CalcMode_t value without updating either the routing table or known_special_cases now causes a test failure. 13/13 ctest pass, 951 assertions. **Complexity delta: `neutral`** (validate function is HOST_TEST-only; no production logic changes).
- 2026-04-30: T3-A arch review — `PrgmOutput_t` callback struct introduced in `prgm_exec.h` (6 slots: `disp_text`, `prog_done`, `clr_home`, `disp_graph`, `show_home`, `input_ready`); `Prgm_SetOutput()` added (always compiled, used by host tests); hardware callbacks (`hw_disp_text/prog_done/clr_home/disp_graph/show_home/input_ready` + `k_hw_output`) defined inside `#ifndef HOST_TEST` block in `prgm_exec.c`; `Prgm_Init()` sets `s_out = &k_hw_output`; all 8 scattered `#ifndef HOST_TEST` display blocks removed from executor functions (`cmd_clrhome`, `cmd_disphome`, `cmd_dispgraph`, `cmd_disp`, `cmd_input`, `prgm_run_loop` ×2, `prgm_run_start`) and replaced with `if (s_out) s_out->callback()`; direct `CalcHistory_Commit` calls in `cmd_disp`/`cmd_input` moved into callbacks; `test_prgm_exec.c` gains `k_test_output` struct with `test_disp_text` calling stub `CalcHistory_Commit` for backward-compat assertions; `prgm_exec_test_stubs.h` comment updated. 11/11 host suites pass, 847 assertions, 119/0 in test_prgm_exec. **Complexity delta: `neutral`** (display calls consolidated into callback table; no new logic; `#ifndef HOST_TEST` scattered guards → single `k_hw_output` struct).
- 2026-04-26: `Shade(` full argument set — `Graph_Shade` upgraded from 2-scalar-constant API to full `(lowerfunc, upperfunc [, resolution, Xbeg, Xend])` per guidebook p. 5-8; `shade_extract_str` depth-aware arg extractor; per-column `Calc_PrepareGraphEquation`+`Calc_EvalGraphEquation` evaluation; boundary curves drawn; resolution column-density; Xbeg/Xend pixel-range clip; optional scalars default from graph state; 15 new group-5e assertions; 10/10 suites pass. **Complexity delta: `neutral`**.
- 2026-04-26: RESET menu (2nd++) — `MODE_RESET_CONFIRM` added to `CalcMode_t`; new `ui_reset.c`/`ui_reset.h` (MEMORY screen: stat pts + program count, 1:No / 2:Reset); `Persist_Reset()` added to `persist.c`/`persist.h` (builds and saves a factory-default `PersistBlock_t`: all vars=0, matrix dims 6×6 all zeros, RANGE defaults, zoom factors=4, mode all-default, stat cleared, Y= cleared); `handle_reset_confirm()` handles TOKEN_1/CLEAR→cancel, TOKEN_2→`Persist_Reset()`+`Prgm_Init()`+`Prgm_Save()`+`srand(0)`, shows "Mem cleared"; TOKEN_RESET wired in `Execute_Token` before dispatch table (accessible from any mode); `Reset_HideScreen()` added to `hide_all_screens()`; `handle_reset_confirm` added to `k_mode_handlers[]` (count 25→26); `ui_init_reset_screen()` added to screen init sequence; `ui_reset.c` added to `CMakeLists.txt`; spec: guidebook p. 1-28. 10/10 suites pass, 831 assertions. **Complexity delta: `neutral`** (new file, 1 mode, no new data structures or render paths beyond the fixed-size screen).
- 2026-04-24: P36–P39 PRGM MODE program commands (full sweep) — new `ui_prgm_mode.c`/`ui_prgm_mode.h` (NUMBER and GRAPH tabs); 2 new `CalcMode_t` values (`MODE_PRGM_MODE_NUMBER`, `MODE_PRGM_MODE_GRAPH`) in `app_common.h`; 17 new cmd handlers in `prgm_exec.c` (NUMBER: `cmd_norm/sci/eng/fix/float/rad/deg`; GRAPH: `cmd_function/param/connected/dot/sequence/simul/grid_off/grid_on/rect/polar`); `Calc_GetNotationMode()`/`Calc_GetDecimalMode()` getters added to `calc_engine.h`/`calc_engine.c`; `TOKEN_MODE` wired in `handle_prgm_editor()` to open NUMBER tab; `prgm_submenu_tab_switch` extended for MODE NUMBER↔GRAPH navigation; 2 mode handler stubs added to `calculator_core_test_stubs.h`; 10 new host assertions in `test_prgm_exec.c` Group 14 (notation/decimal/angle modes; Fix-with-invalid-arg no-op; Sci persists across subsequent expression); `CMakeLists.txt` updated with `ui_prgm_mode.c`. 10/10 suites pass, 752 assertions total. **Complexity delta: `neutral`** (new file; no changes to existing data structures or render paths).
- 2026-04-24: P38 Sequential/Simultaneous plot mode (MODE Row 6) — added `bool plot_sequential` to `GraphState_t`; added `Graph_SetSequentialMode()` accessor in `graph.c`/`graph.h`; wired row 5 ENTER handler in `ui_mode.c`; added restore call in `Persist_ApplyBlock()`; branched both `Graph_Render()` and `Graph_RenderParametric()` on `plot_sequential` (Sequential = complete one curve before the next, Simultaneous = all curves advance one pixel/T-step at a time); 3 new host assertions added to Group 11 in `test_normal_mode.c`; test stub and default state updated in `calculator_core_test_stubs.h`; `MENU_SPECS.md` Row 6 updated to Wired; added P39 PRGM Sequence/Simul backlog; pre-existing `calc_engine.c` `fmt[8]→fmt[16]` `-Werror=format-truncation` fix included. No `PERSIST_VERSION` bump (value already in `mode_committed[5]`). 10/10 ctest pass (742 assertions total). **Complexity delta: `neutral`** (1 bool field, 1 setter, 2 render branches, no new files).
- 2026-04-23: P33 Connected/Dot plot mode (MODE Row 5) — fixed pre-existing Func/Param row index bug (`ui_mode.c` ENTER handler and `persist.c` re-sync were using index 4 instead of 3 for Func/Param); added `bool plot_connected` to `GraphState_t`; added `Graph_SetConnectedMode()` accessor; wired row 4 ENTER handler in `ui_mode.c`; added `Graph_SetConnectedMode()` restore call in `Persist_ApplyBlock()`; branched both `Graph_Render()` and `Graph_RenderParametric()` on `plot_connected` (Connected = interpolated lines, Dot = individual pixels only); added 3 host test assertions (Group 11 in `test_normal_mode.c`); updated `MENU_SPECS.md` rows 4 and 5 to Wired; added P37 PRGM Connected/Dot backlog. No `PERSIST_VERSION` bump (value already in `mode_committed[4]`). 10/10 ctest pass (107 assertions in normal_mode). **Complexity delta: `neutral`** (1 bool field, 1 setter, 2 branch changes; no new files).
- 2026-05-01: [complexity] P29 assessed — stat renderer extraction from `graph.c` declined. `Graph_DrawScatter`, `Graph_DrawXYLine`, `Graph_DrawHistogram`, `stat_plot_prepare`, `draw_line_px` (~155 lines) remain in `graph.c`. Decision: extraction would require 3–4 new public API seams (canvas getter/invalidator, canvas-prepare wrapper, pixel-write interface) because the stat functions write directly into `graph_buf` as the main render pass — unlike `graph_draw.c` which owns a separate overlay buffer composited at render time. Coupling cost exceeds the ~155-line reduction; graph.c would still be ~1137 lines (>500 threshold). Comment block at graph.c:1116 updated to document the rationale. **Complexity delta: `neutral`** (comment-only change; no production logic changes).
- 2026-05-04: Arch-1 — Encapsulate expression editor shared mutable state. `insert_mode`, `cursor_visible`, `sto_pending`, and `expr` made `static` in `calculator_core.c` (with `#ifdef HOST_TEST` guard following the `ans`/`current_mode` pattern); 7 getter/setter functions (`Calc_GetInsertMode`, `Calc_SetInsertMode`, `Calc_GetCursorVisible`, `Calc_SetCursorVisible`, `Calc_GetStoPending`, `Calc_SetStoPending`, `Calc_GetExpr`) declared in `calculator_core.h` (new `expr_util.h` include) and implemented in `calculator_core.c`; 4 `extern` declarations removed from `calc_internal.h` (replaced by the getter/setter API comment); 39 direct-access sites updated across 6 callers (`ui_input.c` 14 sites, `ui_prgm.c` 10 sites, `graph_ui.c` 3 sites, `graph_ui_range.c` 5 sites, `ui_param_yeq.c` 3 sites, `ui_matrix.c` 4 sites); 5 callers had no direct usages (`ui_graph_zoom.c`, `ui_math_menu.c`, `ui_mode.c`, `ui_prgm_ctl.c`, `ui_prgm_io.c`). STO synthesis (`sto_pending ? MODE_STO : current_mode`) preserved unchanged in `calculator_core.c`. 14/14 ctest pass, 964 assertions, 139/139 check_sync. **Complexity delta: `decrease`** (shared mutable coupling surface halved; direct-write access to expression editor state is now a compile error outside `calculator_core.c` in embedded builds).
- 2026-05-01: Arch-5 — `ModeRegistration_t` struct introduced in `calculator_core.c`; `RouteEntry_t`/`RoutePred_t`/`ModeHandler_t` replaced by `ModeRegistration_t`/`ModePredicate_fn`/`ModeHandler_fn`; `mode` field added to the struct; 27 `pred_mode_xxx`/`pred_prgm_running` one-liner functions removed (replaced by `pred = NULL` + mode field); `k_route_table[]` rewritten as array of `ModeRegistration_t` using designated initializers; `Execute_Token()` loop updated (`fires = e->pred ? e->pred(t) : (current_mode == e->mode)`); `calc_mode_topology_validate()` inner loop updated with same NULL-pred logic. net: −84 lines, +67 lines. 14/14 ctest pass, 964 assertions. **Complexity delta: `decrease`** (27 trivial predicate functions eliminated; mode identity now explicit in the struct field rather than implicit in the predicate body).
- 2026-05-01: F1 arch review complete — `extern bool angle_degrees` removed from `calc_internal.h`; last raw read in `ui_prgm.c:1236` migrated to `Calc_GetAngleDegrees()`; `ui_mode.c` write migrated to `Calc_SetAngleDegrees()`; `ui_input.c` two read sites migrated; `prgm_exec.c` seven read sites migrated. All 10 super-module files now access angle mode exclusively through the accessor pair declared in `calculator_core.h`. calc_internal.h header comment updated. 14/14 ctest pass, 964 assertions. **Complexity delta: `decrease`** (raw extern removed; coupling to calculator_core state fully mediated by accessors).
- 2026-05-04: Arch-4 — Isolate program executor from UI layer. All 6 `#ifndef HOST_TEST` direct-call blocks removed from `prgm_exec.c`; `calc_internal.h` include replaced with `calc_history.h` + `calculator_core.h` + `graph.h`; `s_out = &k_hw_output` + all hw callbacks moved out of `prgm_exec.c` into `ui_prgm.c`; `ui_init_prgm_screens()` now calls `Prgm_SetOutput(&k_hw_output)` as adapter registration; two `ExprBuffer_Clear(&expr)` sites (pre-existing Arch-1 gap) fixed via `Calc_GetExpr()`; `format_calc_result` declaration moved from `calc_internal.h` to `calculator_core.h` (with `#include "calc_engine.h"` for `CalcResult_t`); `Calc_GetExpr()` stub added to `prgm_exec_test_stubs.h`. The `PrgmOutput_t` seam is now real — two adapters (test + embedded) both exercised. 14/14 ctest pass, 964 assertions. **Complexity delta: `decrease`** (scattered `#ifdef HOST_TEST` blocks eliminated; executor is now architecture-layer-clean in both builds).
- 2026-05-04: Arch-3 — Generic Menu Screen module for PRGM sub-menus. New `ui_menu_screen.h`/`ui_menu_screen.c` — `MenuScreenDesc_t` (tab bar geometry, static or dynamic item labels, left/right mode targets, on_select/on_cancel/on_tab_switch/on_extra callbacks) + `MenuScreen_t` (owns `MenuState_t`, LVGL screen/labels/scroll indicators); 4 public functions (`MenuScreen_Init`, `ResetAndShow`, `UpdateDisplay`, `HandleToken`). EXEC uses `get_label()` callback for dynamic slot labels and `on_extra()` for TOKEN_A–Z/THETA shortcuts; all four files use `MenuScreen_t`. Line counts (old → new): `ui_prgm_ctl.c` 172→66, `ui_prgm_io.c` 131→63, `ui_prgm_exec.c` 197→88, `ui_prgm_mode.c` 282→108. `ui_prgm.c` unchanged (all external API signatures preserved). `ui_menu_screen.c` added to `CMakeLists.txt`; `TECHNICAL.md` directory map updated. 14/14 ctest pass, 964 assertions. **Complexity delta: `decrease`** (~780 lines of boilerplate removed; shared LVGL/navigation logic now in one testable module).
- 2026-05-04: Arch-2 — Break graph editor circular include chain. Cursor state structs (`TraceState_t`, `FreeCursorState_t`, `ZBoxState_t`) were already in `graph.h`/`graph.c` from T2-C. Remaining cycle: `graph_ui.h` included `graph_ui_range.h` unnecessarily (no types from it used), and `graph_ui_range.c` included `graph_ui.h` with a stale comment (`zoom_menu_reset()` is in `ui_graph_zoom.h`, already included). Two-line fix: (1) removed `#include "graph_ui_range.h"` from `graph_ui.h`; (2) removed `#include "graph_ui.h"` from `graph_ui_range.c`; (3) collapsed duplicate `#include "ui_graph_zoom.h"` in `graph_ui_range.c` to one. All three graph editor files are now independent callers of `graph.h` with no mutual header dependencies. 14/14 ctest pass, 964 assertions. **Complexity delta: `decrease`** (circular dependency removed; graph_ui_range.c is now compilable without graph_ui.c).
- 2026-04-17: COUPLING_REFACTOR T11 (`stat_data`/`stat_results` accessor gap) — `stat_data` and `stat_results` made `static` in `ui_stat.c`; `extern StatData_t stat_data` and `extern StatResults_t stat_results` removed from `app_common.h`; 3 accessors added to `ui_stat.h`/`ui_stat.c` (`Stat_GetData() → const StatData_t *`, `Stat_GetResults() → const StatResults_t *`, `Stat_SetData(const StatData_t *)`); `ui_vars.c` updated — 6 y-stat helper functions migrated to `Stat_GetData()`, `vars_format_value` migrated to `Stat_GetResults()`; `persist.c` gains `#include "ui_stat.h"` inside `#ifndef HOST_TEST` block; `Persist_BuildBlock` reads via `Stat_GetData()`; `Persist_ApplyBlock` builds a temp `StatData_t` with bounds check and calls `Stat_SetData()`; test stubs: `menu_state.h` include hoisted to top of `calculator_core_test_stubs.h` (fixes pre-existing `MenuState_t` ordering bug); `ui_prgm_editor_screen`/`ui_prgm_new_screen` externs added to stubs header (were missing, masked by prior compile error); `test_normal_mode.c` gains those two definitions; `StatData_t stat_data`/`StatResults_t stat_results` definitions removed from `test_normal_mode.c`. Direct `stat_data`/`stat_results` field writes outside `ui_stat.c` are now compile errors. 10/10 ctest pass (694 assertions), firmware builds 0 errors. **Complexity delta: `neutral`** (3 one-liner accessors; no new control flow).

---

## Completed Features

| Feature | Log date | Notes |
|---|---|---|
| **Build** | — | Successful (fixed implicit fallthrough in `graph_ui.c`; startup crash and trace freeze in `graph.c`) |
| **Flash** | — | Successful using OpenOCD to STM32F429I-DISC1 |
| ZBox rubber-band zoom | 2026-03-20 | Final validation on hardware pending |
| UTF-8 cursor navigation | 2026-03-21 | 12 test groups; `expr_util.c` extracted |
| Persist checksums/HAL | 2026-03-21 | FLASH sector 7 guard added; versioned header |
| Graph UI extraction | 2026-03-21 | Removed 1.5k LOC from `calculator_core.c` (P2) |
| Project Update Procedure | 2026-03-21 | Canonical sync rules + workflow automated |
| Global Hard QUIT | 2026-03-22 | 2nd+CLEAR hard exit to main screen implemented |
| Y= Toggle | 2026-03-22 | Equation enable/disable toggle from Y= editor implemented |

---

## Resolved Items

| Item | Summary | Date |
|---|---|---|
| [refactor] PersistBlock_t sub-struct adoption | PERSIST_VERSION 8→9 (2472→2488 B). Five typed sub-structs in `persist.h`: `GraphPersist_t` (696 B), `StatPersist_t` (1204 B), `MatrixPersist_t` (440 B), `PrgmPersist_t` (4 B, reserved), `ModePersist_t` (12 B). Per-section version constants added (all 1U). `Persist_BuildBlock`/`Persist_ApplyBlock`/`Persist_Reset`/`Persist_Save` updated. `test_persist_roundtrip.c` updated (field paths + size 2488). v8 FLASH blocks discarded on first boot (version mismatch); no v8→v9 data migration (one-time structural transition). 14/14 ctest, 964 assertions, 139/139 check_sync; complexity delta: decrease | 2026-05-05 |
| TRACE auto-pan at viewport edge | `else` pan branch added to `TOKEN_LEFT`/`TOKEN_RIGHT` in `handle_trace_mode()` (`graph_ui.c:1096–1143`); pans by `(Xmax−Xmin)/2`, calls `Graph_Render()` then `Graph_DrawTrace()`; param mode unchanged; 14/14 ctest pass; complexity delta: neutral; hardware validation pending | 2026-05-05 |
| Interactive on-graph DRAW Line(/PT cursor-pick | `MODE_GRAPH_DRAW_CURSOR`; `draw_enter_cursor_pick(op)` + `handle_draw_cursor_mode()` in `graph_ui.c`/`.h`; `Graph_DrawLineCursor()` in `graph.c`/`graph.h`; `draw_menu_select()` routes to cursor-pick when `is_graph_context()`; 14/14 ctest pass, 964 assertions; complexity delta: increase (graph_ui.c ~150 lines added; extraction [complexity] item tracked) | 2026-05-05 |
| ZOOM In/Out/Integer cursor-pick mode | `MODE_GRAPH_ZOOM_CURSOR` (graph cursor modes group); `zoom_enter_cursor_pick(op)` + `handle_zoom_cursor_mode()` in `graph_ui.c`/`.h`; reuses ZBox pixel state + `Graph_DrawZBox()` crosshair; ENTER commits zoom centred on picked pixel and stays in mode (re-ENTER re-zooms per guidebook); dead `zoom_scale_view()` and `apply_zoom_preset(5)` ZInteger branch removed; `pred_graph_mode()` + `Graph_HandleKey()` updated; 14/14 ctest pass; complexity delta: neutral | 2026-05-05 |
| Arch-5 `Graph_HandleKey()` unified graph-mode dispatcher | `Graph_HandleKey(Token_t t)` added to `graph.c`/`graph.h`; 8 graph-mode entries in `k_route_table[]` replaced by 1 predicate entry (`pred_graph_mode` + `Graph_HandleKey`); `graph.c` gains UI handler includes guarded by `#ifndef HOST_TEST`; `calculator_core_test_stubs.h` stub added; 14/14 ctest pass, 964 assertions; complexity delta: decrease | 2026-05-05 |
| Arch-1-H Remove `calc_internal.h` | Deleted the umbrella re-export header. Declarations rehomed: `cursor_render`/`cursor_box_create` → `ui_shared.h`; `ui_refresh_display`/`ui_output_row`/`handle_history_nav` → `calculator_core.h`; `zoom_enter_zbox` → `graph_ui.h`. All 10 include sites replaced with targeted includes. 14/14 ctest pass, 964 assertions; complexity delta: decrease | 2026-05-04 |
| Arch-4 Isolate program executor from UI layer | All direct LVGL/history calls removed from `prgm_exec.c`; hw callbacks + `k_hw_output` moved to `ui_prgm.c`; `calc_internal.h` include replaced with targeted `calc_history.h` + `calculator_core.h`; `format_calc_result` declaration moved to `calculator_core.h`; `Calc_GetExpr()` stub added to test stubs; `PrgmOutput_t` seam is now a real two-adapter seam; 14/14 ctest pass, 964 assertions; complexity delta: decrease | 2026-05-04 |
| Arch-3 Generic Menu Screen module | New `ui_menu_screen.h`/`ui_menu_screen.c`; `MenuScreenDesc_t` + `MenuScreen_t`; four PRGM sub-menu files rewritten as descriptor + thin wrappers; total boilerplate removed ~780 lines; EXEC uses dynamic `get_label` + `on_extra` letter shortcuts; external API preserved, `ui_prgm.c` unchanged; 14/14 ctest pass, 964 assertions; complexity delta: decrease | 2026-05-04 |
| Arch-2 circular include chain | Removed `#include "graph_ui_range.h"` from `graph_ui.h`; removed stale `#include "graph_ui.h"` from `graph_ui_range.c`; collapsed duplicate `ui_graph_zoom.h` include; all three graph editor files now independent callers of `graph.h`; 14/14 ctest pass, 964 assertions; complexity delta: decrease | 2026-05-04 |
| Arch-1 expression editor state encapsulation | `insert_mode/cursor_visible/sto_pending/expr` made static in `calculator_core.c`; 7 getter/setters added to `calculator_core.h`; 4 externs removed from `calc_internal.h`; 39 direct-access sites updated in 6 callers; 14/14 ctest pass, 964 assertions; complexity delta: decrease | 2026-05-04 |
| Arch-5 `ModeRegistration_t` | `RouteEntry_t` renamed to `ModeRegistration_t` with explicit `mode` field; 27 one-liner `pred_mode_xxx`/`pred_prgm_running` functions removed; `pred = NULL` dispatches via `current_mode == e->mode`; net −17 lines; 14/14 ctest pass; complexity delta: decrease | 2026-05-01 |
| [complexity] P29 stat renderer extraction | Assessed and declined. Stat renderers (`Graph_DrawScatter`/`XYLine`/`Histogram`, `stat_plot_prepare`, `draw_line_px`, ~155 lines) remain in `graph.c`. Extraction would require 3–4 new public seams (canvas getter, prepare-canvas wrapper, pixel-write interface) because stat functions write directly into `graph_buf` as the main render pass, unlike `graph_draw.c` which owns a separate composited overlay. Comment at graph.c:1116 documents the rationale. | 2026-05-01 |
| T3-B Calc_Parse/Calc_Eval split | `ParsedExpr_t` (postfix + nested nDeriv); `Calc_Parse`/`Calc_Eval` public API; `EvaluateRPN_ex` with `nderiv_eq` param; `nderiv_out` threaded through `Tokenize`/`try_tokenize_identifier`; `graph.c` postfix caches upgraded to `ParsedExpr_t`; 7 prepare+eval call sites in `graph.c` converted; `test_parse_eval.c` (103 assertions, includes nDeriv independence test); 12/12 suites, 950 total; complexity delta: decrease | 2026-04-30 |
| RESET menu (2nd++) | `MODE_RESET_CONFIRM`; `ui_reset.c`/`.h` (MEMORY screen: stat pts + program count, 1:No / 2:Reset); `Persist_Reset()` (factory-default block: vars=0, matrices 6×6 zeros, RANGE defaults, zoom=4, mode defaults, stat cleared, Y= cleared); TOKEN_2 resets calc+prgm+rand, shows "Mem cleared"; TOKEN_1/CLEAR cancels; spec: guidebook p. 1-28; complexity delta: neutral | 2026-04-26 |
| `Dim{x}` VARS DIM tab | `vars_dim_names` expanded from 6 to 7 entries; `vars_tab_item_count[3]` 6→7; `vars_format_value` case 6 reads `Stat_GetData()->list_len` (physical list length per guidebook pp. 6-11, 7-11); `MENU_SPECS.md` updated; build clean, all 10/10 host suites pass; complexity delta: neutral | 2026-04-26 |
| Parametric Y-VARS tabs | Y tab expanded to 10 items (Y₁–Y₄, X₁t, Y₁t, X₂t, Y₂t, X₃t, Y₃t); ON/OFF tabs expanded to 8 items each (All + Y₁–Y₄ + X₁t–X₃t); scroll indicator support added; `yvars_do_enable` handles param pairs idx 5–7 via `Graph_SetParamEnabled`; digit shortcuts TOKEN_6–TOKEN_0; ENTER uses `MenuState_AbsoluteIndex`; tab_move passes `&s->scroll`; complexity delta: neutral | 2026-04-25 |
| `Shade(` full argument set | `Graph_Shade` signature upgraded from `(float yLow, float yHigh)` to `(lower_expr, upper_expr, resolution, x_beg, x_end, fill_color, angle_degrees)`; per-column `Calc_PrepareGraphEquation`+`Calc_EvalGraphEquation`; boundary curves drawn at every column; resolution 1–8 controls fill density; `x_beg`/`x_end` clip to pixel range; `shade_extract_str` depth-aware string-arg extractor in `ui_draw.c`; optional scalars default to graph-state Xres/Xmin/Xmax; 15 new host assertions (group 5e); 10/10 suites, 846 total; complexity delta: neutral | 2026-04-26 |
| `{x}(n)` / `{y}(n)` list-element access | `MATH_FUNC_LIST_X/Y`; `{x}`/`{y}` keyword entries; `Calc_RegisterStatAccessors()` registration pattern; `TOKEN_LIST_X/Y` wired in `ui_input.c`; eval with 1-based index + DOMAIN error; 13 new host assertions (group 5d); 10/10 suites, 831 total | 2026-04-25 |
| Stat data point INS/DEL | `CalcStat_InsertRow/DeleteRow` in `calc_stat.c`; col=2 row-select state + `>` prefix display in `ui_stat_edit.c`; TOKEN_INS inserts (0,0) before cursor row, TOKEN_DEL deletes the row (guidebook p. 7-5); 37 new host assertions; 10/10 suites, 830 total | 2026-04-25 |
| `nDeriv(` numerical derivative | `MATH_FUNC_NDERIV` token + tokeniser hook (pre-compiles arg1 into `s_nderiv_eq`) + symmetric difference quotient evaluator (ε=0.001); 6 new host assertions (group 5c); 10/10 suites, 793 total | 2026-04-25 |
| [bug] `End` subroutine return | `cmd_end()` sets `prgm_run_pc = prgm_run_num_lines`; delegates subroutine pop / top-level termination to existing `prgm_run_loop()` machinery; 12 new host assertions (Group 18); 10/10 suites, 787 total | 2026-04-25 |
| [bug] Auto-close trailing `(` | `sy_drain_stack()` discards unmatched `(` as implicit `)` per guidebook p. 1-9; emits pending function token; 4 new host assertions; 10/10 suites, 775 total | 2026-04-25 |
| Hyperbolic functions engine | 6 enum values, 6 keyword entries, 6 eval cases in `calc_engine.h`/`calc_engine.c`; 16 host assertions; `sinh(/cosh(/tanh(/asinh(/acosh(/atanh(` now evaluate correctly instead of syntax error | 2026-04-25 |
| MATRX row operations in menu | `rowSwap(/row+/(*row(/(*row+(` added as items 1–4; `det(` → 5, `T` → 6; `ui_matrix.c` 3-line change | 2026-04-24 |
| [complexity] Crosshair duplication | `draw_crosshair_px(px, py, c)` helper extracted; ARM=5 loop removed from `Graph_DrawFreeCursor`, `graph_draw_trace_func`, `graph_draw_trace_param`; complexity delta: decrease | 2026-04-24 |
| ZOOM Trig default constants | `Ymin`/`Ymax` ±4→±3, `Yscl` 1.0→0.25 in `ui_graph_zoom.c:205`; matches guidebook p. 3-15 | 2026-04-24 |
| [docs] EE glyph + MATRX extras | `TOKEN_EE` `*10^` deviation documented; `dim(`, `fill(`, `identity(`, `randM(` removed from MATRX MATH tab (TI-82 additions, not in TI-81 spec); item count 6→2 | 2026-04-24 |
| Item 4 (TRACE crosshair) | Free-roaming blinking crosshair + TRACE toggle fix implemented; hardware validation item added | 2026-04-24 |
| P5 | `const` on `TI81_LookupTable` | 2026-03-20 |
| P4 | `ui_palette.h` — 14 named colour constants; inline hex literals replaced | 2026-03-21 |
| P9 | `mat_det` confirmed iterative; tracker entry was incorrect | 2026-03-21 |
| P11 | Duplicate `#include` directives removed from `calculator_core.c` | 2026-03-21 |
| P6 | `-Werror` enabled for App sources | 2026-03-21 |
| P8 | Float printf runtime guard in `App_DefaultTask_Run` | 2026-03-21 |
| P2 | `graph_ui.c` extracted — `calculator_core.c` −66% | 2026-03-21 |
| P16 | FLASH sector map added to `docs/TECHNICAL.md` | 2026-03-21 |
| P12 | Architecture diagram — Mermaid added to `docs/ARCHITECTURE.md` | 2026-03-21 |
| P13 | `docs/TESTING.md` created | 2026-03-21 |
| P17 | `docs/TROUBLESHOOTING.md` created | 2026-03-21 |
| Open-source governance | `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, issue/PR templates, README badges | 2026-03-21 |
| P15 | Expression pipeline walkthrough added to `docs/TECHNICAL.md` | 2026-03-22 |
| P18 | All 10 `CODE_REVIEW_PENDING` items resolved — function complexity C+ → B | 2026-03-22 |
| P19 | `prgm_execute_line` dispatch table — 22 handlers, body 495 → ~30 lines | 2026-03-22 |
| P20 | prgm exec host suite — 121 tests, `prgm_run_loop` bug fixed; Testing B+ → A- | 2026-03-22 |
| P21–P24 | Promoted from retired `CODE_REVIEW_PENDING.md` to CLAUDE.md next priorities | 2026-03-25 |
| P25 | `docs/PRGM_COMMANDS.md` rewritten — 8-CTL/5-IO spec, removed 6 unsupported commands | 2026-04-01 |
| `handle_prgm_editor` split | Extracted 3 static helpers; dispatcher 152 → 40 lines; `ui_prgm.c` | 2026-04-01 |
| `handle_normal_mode` tests | 104-test suite; `calculator_core_test_stubs.h`; HOST_TEST guards; self-copy bug fixed | 2026-04-02 |
| P3 Phase 3 | State params for 4 handlers; `MatrixMenuState_t` public; history buffer 32→1; ~7 KB RAM freed | 2026-04-02 |
| History spec alignment | `HISTORY_LINE_COUNT` 32→1, `MATRIX_RING_COUNT` 8→1; UP/DOWN scroll and `2nd+ENTRY` deviation removed | 2026-04-02 |
| Item 18 | Y= row labels → Y₁–Y₄; TOKEN_X_INV display-only → ⁻¹ U+E001; `^-1` expression insertion kept (Option B); VARS/Y-VARS strings deferred | 2026-04-02 |
| `handle_prgm_menu` split | Extracted `handle_erase_confirm`, `enter_exec_tab`, `enter_edit_tab`, `enter_erase_tab`; dispatcher ~120 → ~50 lines; zero logic change; `graph_ui.c` implicit-fallthrough fixed | 2026-04-02 |
| P34 | Parametric graphing — `MATH_VAR_T`, `Calc_PrepareParamEquation`/`Calc_EvalParamEquation`, `GraphState_t` param fields, persist v5, `Graph_RenderParametric`/`Graph_InvalidateCache`, param Y= screen + 9-field RANGE + `handle_param_yeq_mode`, `TOKEN_X_T` + MODE row 4 + `MODE_GRAPH_PARAM_YEQ` dispatch; 28-test `test_param.c`; hardware validation pending (P35h) | 2026-04-02 |
| P30 | STAT menu — `StatData_t`/`StatResults_t` in `app_common.h`; `calc_stat.c` (1-Var, LinReg, LnReg, ExpReg, PwrReg, SortX, SortY, Clear); persist v6 (+796 B stat list); `ui_stat.c` (3 screens: tab-menu, DATA editor, results); `Graph_DrawScatter`/`XYLine`/`Histogram` in `graph.c`; `TOKEN_STAT` dispatch in `calculator_core.c`; 39-test `test_stat.c`; hardware validation pending (P30h) | 2026-04-03 |
| P29 | DRAW menu — `MODE_DRAW_MENU`; `ui_draw.h`/`ui_draw.c` (7-item menu: ClrDraw immediate + 6 expression-insert commands); persistent draw layer in SDRAM (0xD0080800, 150 KB, transparent-sentinel); `Graph_DrawLayer*` + `apply_draw_layer` + coordinate helpers in `graph.c`; `try_execute_draw_command` + TOKEN_DRAW dispatch in `calculator_core.c`; HOST_TEST stubs added; hardware validation pending (P29h) | 2026-04-04 |
| P31 | VARS menu — `MODE_VARS_MENU`; `ui_vars.h`/`ui_vars.c` (5-tab: XY/Σ/LR/DIM/RNG, 10-item RNG tab with scroll); inserts current value of selected variable; on-the-fly y-stats from `stat_data`; TOKEN_VARS in `handle_normal_mode` + `handle_yeq_mode`; hardware validation pending | 2026-04-04 |
| P32 | Y-VARS menu — `MODE_YVARS_MENU`; `TOKEN_Y1–Y4`; `MATH_VAR_Y1–Y4` + `Calc_RegisterYEquations` + ShuntingYard/ImplicitMulPass support in `calc_engine.c`; `ui_yvars.h`/`ui_yvars.c` (3-tab Y/ON/OFF; Y inserts "Y₁"–"Y₄" ref strings; ON/OFF toggle `graph_state.enabled[]`); reentrancy guard; TOKEN_Y_VARS dispatch; 20-test `test_yvars.c`; hardware validation pending (P32h) | 2026-04-15 |
| REFACTOR Item 1 | ui_prgm.c sub-menu extraction — CTL/IO/EXEC into 3 new modules; ui_prgm.c 1652 → 1276 lines | 2026-04-15 |
| REFACTOR Item 2 | ExprBuffer_t wrapper — 3 raw globals → struct; expr_buffer.c; test_expr_buffer.c (48 assertions) | 2026-04-16 |
| REFACTOR Item 3 | MenuState_t navigation helpers — menu_state.h/menu_state.c; ui_vars.c proof-of-concept migration; VarsMenuState_t removed; test_menu_state.c (43 assertions); TODO notes added to 5 remaining menus | 2026-04-16 |
| REFACTOR Item 4 | ZOOM menu extraction — ui_graph_zoom.h/ui_graph_zoom.c; graph_ui.c 1131 → 862 lines; 10 functions moved | 2026-04-16 |
| REFACTOR Item 5 | graph_state ownership documentation — comment block in app_common.h; State Ownership section in TECHNICAL.md | 2026-04-16 |
| REFACTOR Item 6 | PersistBlock_t sub-struct split design — Persist Migration Design section in TECHNICAL.md; proposed GraphPersist_t/StatPersist_t/MatrixPersist_t/PrgmPersist_t/ModePersist_t layout documented; adopt at next version bump | 2026-04-16 |
| INTERFACE_REFACTOR_PLAN | All 6 items complete — structural refactoring milestone finished | 2026-04-16 |
| COUPLING_REFACTOR T1 | graph_state accessor layer — static in graph.c; 13 accessors; extern removed from app_common.h; 9 modules migrated; ui_graph_zoom.c + menu_state.c added to CMakeLists; API/header design A → A+ | 2026-04-16 |
| COUPLING_REFACTOR T2 | g_prgm_store accessor API — 7 accessors (GetName/GetBody/IsSlotOccupied/SetName/AppendLine/SetBody/ClearSlot); extern removed from prgm_exec.h; ui_prgm.c + ui_prgm_exec.c + both test stubs migrated; API/header design remains A+ | 2026-04-16 |
| COUPLING_REFACTOR T7 | Execute_Token dispatch table — 22-entry `k_mode_handlers[]` replaces sequential if-chain; 3 thin wrappers for non-uniform signatures; `_Static_assert` guards count; ALPHA_LOCK compound cases kept explicit; complexity decrease | 2026-04-17 |
| COUPLING_REFACTOR T10 (ui_yvars) | MenuState_t retrofit — YVarsMenuState_t removed; item_cursor→cursor; MenuState_MoveUp/Down replaces manual bounds checks; test stubs updated; 4 modules remain | 2026-04-17 |
| COUPLING_REFACTOR T10 (ui_math_menu, ui_matrix, ui_stat, ui_draw) | MenuState_t retrofit complete — MathMenuState_t/TestMenuState_t/MatrixMenuState_t/StatMenuState_t/DrawMenuState_t removed; all 5 remaining modules migrated; all bespoke UP/DOWN logic replaced with helpers; test stubs updated; COUPLING_REFACTOR.md deleted | 2026-04-17 |
| COUPLING_REFACTOR (all 10 tasks) | Full coupling refactor milestone complete — 10 tasks across 3 tiers; calc_internal.h externs eliminated; all shared state encapsulated behind accessor APIs; COUPLING_REFACTOR.md deleted | 2026-04-17 |
| COUPLING_REFACTOR T5 | Mode transition via Calc_SetMode() — 4 accessors in calculator_core.h; current_mode/return_mode static in calculator_core.c; extern removed from calc_internal.h; 13 files migrated; direct writes outside calculator_core.c are now compile errors; API/header design remains A+ | 2026-04-17 |
| REVIEW Item 1 | Cursor overlay dispatch deduplication — `Prgm_IsEditorScreenVisible`/`Prgm_IsNewScreenVisible` added to `ui_prgm.c/h`; raw externs removed from `ui_prgm.h`; `update_overlay_cursor()` helper extracted in `calculator_core.c`; test stubs cleaned up | 2026-04-18 |
| COUPLING_REFACTOR T11 | stat_data/stat_results accessor gap — stat_data/stat_results made static in ui_stat.c; extern declarations removed from app_common.h; 3 accessors added (Stat_GetData/Stat_GetResults/Stat_SetData) to ui_stat.h; ui_vars.c and persist.c migrated to use accessors; test stubs updated (menu_state.h include order fixed; prgm_editor/new_screen externs added); direct field writes outside ui_stat.c are now compile errors; API/header design remains A+; complexity delta: neutral | 2026-04-17 |
| REVIEW Item 7 | graph.c three functions over 80 lines — `graph_draw_trace_param`/`graph_draw_trace_func` extracted from `Graph_DrawTrace()` (118→17 lines); `graph_render_setup()` eliminates 4-line fill_bg/grid/axes/ticks duplication between both render functions; `GRAPH_BUF_ADDR` named constant replaces raw `0xD0025800`; firmware clean, 10/10 tests pass; complexity delta: decrease | 2026-04-17 |
| REVIEW Item 3 | `ui_refresh_display` inner loop restructure — replaced O(rows×entries) nested walk with a flat `line_map[DISP_ROW_COUNT]` built in one forward pass; render loop is now O(1) per row; complexity ceiling drops from 3 → 2 open items; zero behaviour change; all 10 host test suites pass | 2026-04-17 |
| REVIEW Item 4 | `prgm_token_to_str` lookup table — replaced 35-case switch with `k_prgm_tok_strs[]` struct array + single-loop; token/string pairs contiguous in source; tokens not contiguous in enum so offset array not viable; zero behaviour change; build clean | 2026-04-17 |
| REVIEW Item 8 | `eval_matrix_func` branch extraction — 8 if-branches each pulled into a named static helper (`eval_mat_transpose`, `eval_mat_det`, `eval_mat_rowswap`, `eval_mat_rowplus`, `eval_mat_mrow`, `eval_mat_mrowplus`, `eval_mat_round`, `eval_mat_arith`); `eval_matrix_func` reduced to 8-line dispatch; `MAT_ERR` file-scoped macro replaces per-function `MERR`; zero behaviour change; all 10 host suites pass; complexity delta: decrease | 2026-04-18 |
| REVIEW Item 5 | `Process_Hardware_Key` direct mode mutation — all direct `current_mode`/`return_mode` reads and writes replaced with `Calc_GetMode()`/`Calc_SetMode()`/`Calc_GetReturnMode()`/`Calc_SetReturnMode()` accessor calls throughout `Process_Hardware_Key()`; already applied as part of COUPLING_REFACTOR T5; zero behaviour change | 2026-04-18 |
| REVIEW Item 6 | `ui_vars.c` y-statistics — `CalcStat_SumY`, `CalcStat_SumY2`, `CalcStat_SumXY`, `CalcStat_MeanY`, `CalcStat_SxY`, `CalcStat_SigmaY` added to `calc_stat.c`/`calc_stat.h`; five `vars_*` static helpers in `ui_vars.c` replaced with `CalcStat_*` calls via `Stat_GetData()`; `test_stat.c` covers new functions with known dataset and empty-list edge cases; `REVIEW_FOLLOWUP_2026-04-17.md` deleted (all 8 items resolved) | 2026-04-18 |
| #6 ZBox render speed | `Graph_DrawZBox`: per-pixel `lv_canvas_set_px` → direct `graph_buf[]` writes + single `lv_obj_invalidate`; eliminates ~200+ redundant LVGL calls per keypress (hardware waiver: mechanical rewrite — same pixel values, same bounds checks, same visual output; 2026-04-04) | 2026-04-04 |
| P33 Connected/Dot | MODE Row 5 wired — `plot_connected` field, `Graph_SetConnectedMode()`, render branches in `Graph_Render()` and `Graph_RenderParametric()`; Func/Param row index bug fixed; persist restore added; 3 host test assertions | 2026-04-23 |
| STAT_MAX_POINTS 99→150 | Guidebook p. 7-2 spec; PERSIST_VERSION 7→8; persist struct 2060→2472 B; stack OK; 10/10 tests pass | 2026-04-24 |
| P38 Sequential/Simul | MODE Row 6 wired — `plot_sequential` field, `Graph_SetSequentialMode()`, sequential/simultaneous render branches in `Graph_Render()` and `Graph_RenderParametric()`; persist restore added; 3 host test assertions; 742 total | 2026-04-24 |
| P36–P39 PRGM MODE commands | `ui_prgm_mode.c` (NUMBER/GRAPH tabs); 17 cmd handlers in `prgm_exec.c`; `MODE_PRGM_MODE_NUMBER/GRAPH`; `TOKEN_MODE` wired in editor; `Calc_GetNotationMode/DecimalMode` getters; 10 host assertions; 752 total | 2026-04-24 |
| P33 Normal/Sci/Eng notation | MODE Row 1 wired — `Calc_SetNotationMode()` (0=Normal/1=Sci/2=Eng), `calc_notation_mode` static in `calc_engine.c`; `Calc_FormatResult()` restructured into Normal/Sci/Eng branches; Normal auto-sci threshold corrected to 1e-3 per guidebook §1-15; `ui_mode.c` ENTER row 0 → `Calc_SetNotationMode()`; `persist.c Persist_ApplyBlock()` restores on load; test Groups 13/20 updated, Group 21 added (30 assertions); host test stub + test assertion corrected (2026-04-24) | 2026-04-23 |

---

## Milestone Reviews

| Date | Notes |
|---|---|
| 2026-03-20 | Initial review; P1–P10 logged; rating 60–70% |
| 2026-03-21 | Testing D → C → B → B+ across multiple sessions. Full quality pass; header audit A-grade. Rating 80–88%. |
| 2026-03-21 | Sessions 6–10: P2/P6/P8/P12/P13/P16/P17 resolved. `-Werror` enabled; Architecture/Testing/Troubleshooting docs created. Rating 88–92%. |
| 2026-03-22 | Sessions 11–13: Y= toggle, persist v4, matrix history display. RAM 82.44%. |
| 2026-03-22 | Session 15: P18 — function complexity C+ → B. Rating 92–94%. |
| 2026-03-22 | Sessions 16–18: Graph postfix cache; matrix ring buffer; LVGL heap → SDRAM. RAM 48.49%. |
| 2026-03-22 | Sessions 19–23: PRGM feature-complete (~95%); dispatch table (P19); 121-test exec suite (P20). Testing B+ → A-. Rating 93–95%. |
| 2026-03-25 | Sessions 26–28: PRGM spec alignment −44 tests (suite 378/378); manual test plan 40 → 50; `CODE_REVIEW_PENDING.md` retired; P21–P24 promoted. |
| 2026-03-26 | `QUALITY_TRACKER.md` renamed to `MAINTENANCE_STANDARDS.md`; restructured as process document; history moved here. |
| 2026-04-01 | P28 complete: `cursor_place()` → `cursor_render()`. All 7 cursor-update functions now pass explicit `visible`/`mode`/`insert` params; no global state read. `MODE_STO` synthetic enum added. API/header design A- → A. Rating 90–92%. Complexity delta: **decrease**. |
| 2026-04-01 | Refactor+docs batch (session 30): P21–P27 static helper extractions across graph_ui.c/calculator_core.c/app_init.c; MAINTENANCE_STANDARDS.md/TECHNICAL.md stale-reference cleanup; P1 property-based tests (sin²+cos²=1 ×1000, Calc_FormatResult sci-notation boundaries). Testing A- → A. Suite 412/412. Rating 91–93%. |
| 2026-04-02 | `handle_normal_mode` host tests: 104 tests / 10 groups; calculator_core_test_stubs.h stub layer; self-copy bug fixed in commit_history_entry. Suite 412 → 516. Testing remains A. Rating 91–93%. |
| 2026-04-02 | P3 Phase 3 complete: state params for 4 handlers; `MatrixMenuState_t` public; history buffer 32→1; ~7 KB RAM freed; TI-81 history deviation resolved. Complexity delta: **decrease**. Rating 91–93%. |
| 2026-04-16 | INTERFACE_REFACTOR_PLAN all 6 items complete. No scorecard dimension changed (all items complexity-neutral; code organisation targets not met: ui_prgm.c 1268 lines vs 1000 target, graph_ui.c 862 lines vs 800 target). Follow-on work logged in CLAUDE.md: MenuState_t retrofit for 5 remaining menus, graph_state accessor layer, PersistBlock_t sub-struct adoption at next version bump. INTERFACE_REFACTOR_PLAN.md retired. Rating 91–93% (unchanged). |
| 2026-04-16 | COUPLING_REFACTOR T1 complete. `graph_state` encapsulated as static in `graph.c`; all external writes via `Graph_*` accessor functions; `extern GraphState_t graph_state` removed from `app_common.h`. API/header design A → A+. Reviewer: Claude Code (claude-sonnet-4-6). |
| 2026-04-17 | COUPLING_REFACTOR all 10 tasks complete. All shared internal state encapsulated behind accessor APIs: `graph_state` (T1), `g_prgm_store` (T2), `ans`/`ans_is_matrix` (T3), LVGL screen pointers (T4), `current_mode`/`return_mode` (T5), menu open/close delegation (T6), Execute_Token dispatch table (T7), calc_history extracted (T8), persist logic moved (T9), MenuState_t retrofit for all 6 menu modules (T10). `calc_internal.h` extern block eliminated. `COUPLING_REFACTOR.md` deleted. No scorecard dimension changed from T1 completion (API/header design remains A+). Reviewer: Claude Code (claude-sonnet-4-6). |

---

## Scorecard Change Log

| Date | Dimension | Old | New | Trigger |
|---|---|---|---|---|
| 2026-04-03 | Testing | B+ | A | P1 property-based invariant tests + handle_normal_mode coverage added |
| 2026-04-15 | Code organisation | B | B | ui_prgm.c extracted CTL/IO/EXEC sub-menus → 1652→1276 lines; still over 500-line threshold |
| 2026-04-16 | API / header design | A | A | ExprBuffer_t wrapper added (Item 2); three raw globals → single struct; complexity delta: neutral |
| 2026-04-16 | Testing | A | A | MenuState_t helpers added (Item 3); test_menu_state.c 43 assertions; suite grows to 10 suites / 694 assertions; complexity delta: neutral |
| 2026-04-16 | Code organisation | B | B | ZOOM menu extracted graph_ui.c → ui_graph_zoom.c (1131→862 lines); <800 target missed by 62 lines (zoom_enter_zbox kept in graph_ui.c for s_zbox ownership); complexity delta: neutral |
| 2026-04-16 | API / header design | A | A+ | COUPLING_REFACTOR T1: graph_state static in graph.c; extern removed from app_common.h; 13 accessors; direct field writes outside graph.c are now compile errors; complexity delta: neutral |
| 2026-04-16 | API / header design | A+ | A+ | COUPLING_REFACTOR T2: g_prgm_store extern removed from prgm_exec.h; 7 accessors; direct field writes in ui_prgm.c + ui_prgm_exec.c eliminated; complexity delta: neutral |
| 2026-04-16 | API / header design | A+ | A+ | COUPLING_REFACTOR T3: ans/ans_is_matrix static in calculator_core.c; 4 accessors; format_calc_result signature simplified; complexity delta: neutral |
| 2026-04-17 | API / header design | A+ | A+ | COUPLING_REFACTOR T4: 16 LVGL screen pointers made static; extern block removed; 25 show/hide one-liners added; complexity delta: neutral |
| 2026-04-17 | API / header design | A+ | A+ | COUPLING_REFACTOR T5: current_mode/return_mode static in calculator_core.c; 4 accessors; direct writes across 13 files eliminated; complexity delta: neutral |
| 2026-04-17 | Code organisation | B | B | COUPLING_REFACTOR T8: history ring buffer extracted into calc_history.c (~150 lines); calculator_core.c reduced ~150 lines; complexity delta: neutral |
| 2026-04-17 | Code organisation | B | B | COUPLING_REFACTOR T9: Persist_BuildBlock/ApplyBlock moved into persist.c; calculator_core.c reduced 1544→1453 lines; complexity delta: neutral |
| 2026-04-17 | API / header design | A+ | A+ | COUPLING_REFACTOR T11: stat_data/stat_results static in ui_stat.c; 3 accessors added; complexity delta: neutral |
| 2026-04-24 | Code organisation | B | B | P30 follow-up: ui_stat_edit.c extracted from ui_stat.c (703→367 lines); complexity delta: decrease |
| 2026-04-24 | Testing | A | A | P36–P39 PRGM MODE commands: 17 cmd handlers, ui_prgm_mode.c, 10 assertions; 752 total; complexity delta: neutral |
| 2026-04-25 | Testing | A | A | Hyperbolic functions: 6 tokens, 16 assertions; 771 total; complexity delta: neutral |
| 2026-04-25 | Error handling | A- | A- | Auto-close trailing `(`: sy_drain_stack() discards unmatched `(`; 4 assertions; complexity delta: neutral |
| 2026-04-25 | Testing | A | A | `End` subroutine return fix; 12 assertions (Group 18); 787 total; complexity delta: neutral |
| 2026-04-25 | Testing | A | A | nDeriv( numerical derivative; 6 assertions (group 5c); 793 total; complexity delta: neutral |
| 2026-04-25 | Testing | A | A | Stat INS/DEL: CalcStat_InsertRow/DeleteRow; 37 assertions; 830 total; complexity delta: neutral |
| 2026-04-25 | Testing | A | A | {x}(n)/{y}(n) list-element access; Calc_RegisterStatAccessors(); 13 assertions (group 5d); 831 total; complexity delta: neutral |
| 2026-04-25 | Code organisation | B | B | Parametric Y-VARS tabs expanded; scroll indicators; complexity delta: neutral |
| 2026-04-26 | Code organisation | B | B | Dim{x} VARS DIM tab item 7; complexity delta: neutral |
| 2026-04-26 | Code organisation | B | B | RESET menu (2nd++): ui_reset.c/h, Persist_Reset(), k_mode_handlers[] 25→26; complexity delta: neutral |
| 2026-04-26 | Testing | A | A | Shade( full argument set: 5-arg form, per-column eval, boundary curves, clip region; 15 assertions (group 5e); 846 total; complexity delta: neutral |
| 2026-04-30 | Testing | A | A | T1-A arch review: prgm_cmd_table_validate() + test_prgm_cmd_table.c; 847 total; complexity delta: neutral |
| 2026-04-30 | Documentation | A- | A- | T1-B arch review: CalcMode_t topology documented in app_common.h; comment-only change; complexity delta: neutral |
| 2026-04-30 | Function complexity | B | B | T2-A arch review: Execute_Token flattened into k_route_table[] (35 RouteEntry_t rows); ModeEntry_t/k_mode_handlers[] removed; _Static_assert removed; 6 route_* wrappers + 31 pred_* predicates; Execute_Token body is a 4-line loop; all 11 host suites / 847 assertions pass; complexity delta: decrease |
| 2026-04-30 | API / header design | A+ | A+ | T2-B arch review: angle_degrees param removed from 4 graph render/draw functions; 25 call sites cleaned; complexity delta: decrease |
| 2026-04-30 | Code organisation | B | B | T2-C arch review: TraceState_t/FreeCursorState_t/ZBoxState_t + instances moved from graph_ui.c to graph.c; 12 accessor functions added to graph.h/graph.c; TRACE auto-pan backlog now a one-file change; complexity delta: neutral |
| 2026-04-30 | Code organisation | B | B | T3-A arch review: PrgmOutput_t callback struct added; Prgm_SetOutput() + 6 hw_ callbacks in prgm_exec.c; all scattered #ifndef HOST_TEST display guards replaced; test_prgm_exec.c gains k_test_output; calculator_core_test_stubs.h 546→unchanged (hardware callbacks are self-contained); complexity delta: neutral |
