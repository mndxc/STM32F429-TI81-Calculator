# Project History

The single history archive for this project. Add a Session Log entry after every session. Add Completed Features / Resolved Items rows when work ships. Add a Milestone Reviews entry only when a rating changes or a major milestone is reached.

---

## Session Log

- 2026-03-20: PRGM UI polish, colour palette extraction (`ui_palette.h`), PRGM module extraction to `ui_prgm.c`
- 2026-03-21: `expr_util.c` extraction (9 pure functions), 301-test host suite, persist round-trip tests, HAL guards in `persist.c`, full quality review pass
- 2026-03-21 (Session 6): `graph_ui.c` extraction (P2), float printf runtime guard (P8), FLASH sector map docs (P16)
- 2026-03-21 (Session 7): Integrate project update procedure (workflow, documentation, guidelines)
- 2026-03-21 (Session 8): Implement Global Hard QUIT (2nd+CLEAR) navigation
- 2026-03-21 (Session 9): IDE/Build fixes (IntelliSense header fix, recursive include resolve, debug config fix, CMake build fix)
- 2026-03-22 (Session 10): P6, P12, P13, P17 resolved (Sweet Spot items). -Werror enabled for App; Architecture/Testing/Troubleshooting docs created.
- 2026-03-22 (Session 11): Implement Y= equation enable/disable toggle functionality. Update graph renderer and persistence (v4). Fixed a startup crash and multiple trace/graph transition freezes in `graph.c` and `graph_ui.c` (guards for zero-scale ticks, loop clamping for singularities, and LVGL mutex synchronization).
- 2026-03-22 (Session 12): [P15] Expression pipeline documented in `TECHNICAL.md` with "2 + sin(45)" worked example. `MAINTENANCE_STANDARDS.md` updated.
- 2026-03-22 (Session 13): Matrix history display refactored — column-aligned rows, horizontal scroll via LEFT/RIGHT when expression is empty, `<`/`>` clip indicators. `HistoryEntry_t` now embeds `CalcMatrix_t` copy. Build at 82.44% RAM.
- 2026-03-22 (Session 15): P18 resolved — all 10 CODE_REVIEW_PENDING items complete. PRGM execution logic moved from `ui_prgm.c` to `prgm_exec.c` (−545 L / +540 L). Over-100-line functions split: `ShuntingYard` (3 helpers), `handle_yeq_mode` (navigation+insertion), `ui_init_graph_screens` (4 per-screen), `handle_history_nav` (`commit_history_entry`), `ui_refresh_display` (`render_result_row`), `try_tokenize_number` (2 helpers). Docs: README links, ARCHITECTURE diagram, `calc_internal.h` scope comment. `CODE_REVIEW_PENDING.md` deleted. Function complexity C+ → B. 301/301 tests pass. **Complexity delta: `decrease`**.
- 2026-03-22 (Session 16): Graph render speed — added `MATH_VAR_X` token and `GraphEquation_t` postfix cache API to `calc_engine`. `Graph_Render` now runs Tokenize+ShuntingYard once per equation per render (on equation change only) and calls `Calc_EvalGraphEquation` per pixel column. Parse cost 320× → 1× per equation per frame. 301/301 tests pass. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 17): `HistoryEntry_t` matrix ring buffer refactor — replaced embedded `CalcMatrix_t` (148 B) with 3-byte ring reference. 8-slot `matrix_ring[]` stores last 8 matrix results; generation counter detects eviction and falls back to pre-formatted `result` string. RAM: 82.44% → 81.82%. 301/301 tests pass. **Complexity delta: `decrease`**.
- 2026-03-22 (Session 18): RAM audit (P12) — root cause: LVGL heap + FreeRTOS heap = 128 KB = 65% of 192 KB internal RAM; SDRAM had 63.5 MB free. Fix: `SDRAM` region added to linker script, `.sdram (NOLOAD)` section, `LV_ATTRIBUTE_LARGE_RAM_ARRAY` redirects LVGL heap to SDRAM. RAM: 81.82% → 48.49%. 301/301 tests pass. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 19): PRGM system feature-complete — `IS>(` and `DS<(` implemented; `DispHome`, `DispGraph`, `Output(`, `Menu(` implemented. PRGM: ~50% → ~95% (hardware validation pending, P10). 301/301 tests pass. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 20): PRGM command reference created — `docs/PRGM_COMMANDS.md`. `docs/PRGM_COMPLETION.md` deleted. 301/301 tests pass. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 21): Periodic code review — structural scan + Phase 2 direct reads. P19/P20 opened. VARS/Y-VARS menu specs added to CLAUDE.md. **Complexity delta: `neutral`**.
- 2026-03-22 (Session 22): P19 resolved — `prgm_execute_line` dispatch table refactor. 22 static `cmd_*` handler functions extracted; dispatch table replaces 495-line if/else chain; body reduced to ~30 lines. Zero logic changes. 301/301 tests pass. **Complexity delta: `decrease`**.
- 2026-03-22 (Session 23): P20 resolved — program execution host test suite. `#ifndef HOST_TEST` guards added throughout `prgm_exec.c`/`.h`; 121 tests / 14 groups. Bug fixed: `prgm_run_loop` Stop/Return/Goto-abort path did not reset `current_mode`. Testing B+ → A-. 422/422 tests pass. **Complexity delta: `neutral`**.
- 2026-03-25 (Session 27): PRGM manual test plan rewrite — comprehensive 50-test plan targeting Session 26 regressions. Test count: 40 → 50. **Complexity delta: `neutral`**.
- 2026-03-25 (Session 26): PRGM hardware test fixes — all 5 groups (A–E) executed. CTL menu → 8 items; I/O menu → 5 items; removed Then/Else/While/For/Return/Prompt/Output(/Menu( per spec; EXEC tab execution model implemented. Test suite: 378/378 pass (−44 tests for removed commands). Firmware: 48.45% RAM, 36.28% FLASH. **Complexity delta: `decrease`**.
- 2026-03-26 (Session 28): `QUALITY_TRACKER.md` renamed to `MAINTENANCE_STANDARDS.md`; restructured as process document; history moved to `PROJECT_HISTORY.md`; session log consolidated from `CLAUDE.md`. **Complexity delta: `neutral`**.
- 2026-03-27 (Session 29): PRGM hardware test fix groups F1–F10 — 9 failing tests addressed. F1: CLEAR aborts infinite loop (`prgm_request_abort()` called from keypadTask + `osDelay(0)` per line in `prgm_run_loop`). F2: `DispGraph` deadlock fixed (`osDelay(20)` before `Graph_Render` per gotcha #16). F3: EXEC tab added to in-editor CTL/IO sub-menu — slot picker screen, 3-way tab navigation, subroutine insertion (`prgmNAME`); new `MODE_PRGM_EXEC_MENU`. F4: TEST/MATH menus now accessible from program editor; insert redirected via `prgm_editor_menu_insert()`. F5: Name-entry DEL re-engages ALPHA; ALPHA_LOCK routing fixed. F6/F7: ALPHA+letter and digit slot shortcuts work in all 3 PRGM tabs. F8: INS toggles insert mode in editor; underscore cursor shape for insert. F9: `Input` prompt always shows `?` only. F10: Arrow navigation in name-entry (`prgm_new_name_cursor`); LEFT/RIGHT move in field; DOWN opens editor body; UP at editor line 0 returns to name entry. 378/378 tests pass. Firmware: 48.48% RAM, 36.45% FLASH. **Complexity delta: `increase`** (`ui_prgm.c` grew ~200 lines: new EXEC sub-menu screen + handler + cursor state; see complexity item in CLAUDE.md).
- 2026-03-31 (Session 30): Cursor insert+modifier fix — `LV_OBJ_FLAG_OVERFLOW_VISIBLE` on cursor box; `in_insert` condition simplified (removed `current_mode` guards) so mode indicator (^/A) renders above the underline cursor in insert+2ND/ALPHA. `handle_yeq_insertion` in `graph_ui.c` refactored to use `ExprUtil_InsertChar`/`InsertStr` (−20 lines duplicate logic). `test_prgm_exec` expanded by 18 tests (empty body, slot lookup, 2-level nesting): 378 → 396 total. P26/P27/P28 refactor items added to CLAUDE.md. Full doc-sync pass: test counts corrected in `GETTING_STARTED.md`, `README.md`, `TECHNICAL.md`. **Complexity delta: `decrease`** (graph_ui.c deduplication + calculator_core.c simplification).
- 2026-04-01: P25 resolved — `docs/PRGM_COMMANDS.md` rewritten to match post-Session-26 8-CTL/5-IO spec. Removed Then/Else/While/For(/Return/Prompt/Output(/Menu( entries; updated If (single-line only), Goto/Lbl (single-char constraint), Input (shows `?`), End (no-op note); documented EXEC tab `prgm<name>` insertion model; removed stale Limits row. **Complexity delta: `neutral`**.
- 2026-04-01: `handle_prgm_editor` split — extracted `prgm_editor_handle_nav`, `prgm_editor_handle_del_clear`, `prgm_editor_handle_insert` as three focused static helpers in `ui_prgm.c`. Main dispatcher reduced from 152 → 40 lines. Zero logic change. **Complexity delta: `neutral`**.
- 2026-04-02: `handle_normal_mode` host test suite — 104 tests / 10 groups covering all 8 static sub-handlers. `calculator_core_test_stubs.h` (comprehensive LVGL/RTOS/graph_ui/ui_matrix/ui_prgm stub layer), minimal HOST_TEST guards in `calculator_core.c` (include block, `sto_pending` visibility, `lvgl_lock` bodies). Bug fixed: `commit_history_entry` self-copy when re-evaluating only history slot (pointer equality guard). Suite 412 → 516 total. **Complexity delta: `neutral`**.
- 2026-04-02: Item 18 display-string replacements — Y= row labels → Y₁–Y₄ (`graph_ui.c:154`); TOKEN_X_INV display-only mappings → ⁻¹ U+E001 (`graph_ui.c`, `ui_prgm.c`). Expression buffer `^-1` kept as intentional deviation (Option B). VARS/Y-VARS strings deferred to when those menus are implemented. **Complexity delta: `neutral`**.
- 2026-04-02: `handle_prgm_menu` split — extracted `handle_erase_confirm`, `enter_exec_tab`, `enter_edit_tab`, `enter_erase_tab` as focused static helpers in `ui_prgm.c`. Dispatcher ~120 → ~50 lines. Zero logic change. `graph_ui.c` pre-existing implicit-fallthrough `-Werror` fixed (`__attribute__((fallthrough))`). **Complexity delta: `neutral`**.
- 2026-04-02: P3 Phase 3 complete — `handle_mode_screen`, `handle_math_menu`, `handle_test_menu`, `handle_matrix_menu` now accept explicit state struct pointers (`ModeScreenState_t *`, `MathMenuState_t *`, `TestMenuState_t *`, `MatrixMenuState_t *`) instead of reading module-level statics. `MatrixMenuState_t` moved from `calculator_core.c` to `ui_matrix.h` (now public extern `matrix_menu_state`); the three former `matrix_tab`/`matrix_item_cursor`/`matrix_return_mode` statics replaced by the consolidated struct. History buffer reduced to TI-81 spec: `HISTORY_LINE_COUNT` 32 → 1, `MATRIX_RING_COUNT` 8 → 1 (~7 KB RAM recovered). Removes the two "Deliberate Deviations" for UP/DOWN multi-step history scroll and `2nd+ENTRY` offset behaviour. Zero logic change — circular buffer math and `handle_history_nav` work correctly at size 1. **Complexity delta: `decrease`** (RAM reduction, state consolidation, handler testability).
- 2026-04-02: P34 parametric graphing complete — `MATH_VAR_T` token + `Calc_PrepareParamEquation`/`Calc_EvalParamEquation` API in `calc_engine`; `GRAPH_NUM_PARAM 3` pairs + `t_min/max/step` + `param_mode` in `GraphState_t`; `PersistBlock_t` v5 (864 → 1264 bytes); `Graph_RenderParametric` + `Graph_InvalidateCache` in `graph.c`; `ui_param_yeq_screen` + `handle_param_yeq_mode` + 9-field parametric RANGE layout in `graph_ui.c`; MODE row 4 handler, `TOKEN_X_T` mode-sensitive insert, `MODE_GRAPH_PARAM_YEQ` dispatch in `calculator_core.c`; 28-assertion `test_param.c` host test suite. 516 → 449 total (prior count was assertions, now tracked per-suite). **Complexity delta: `increase`** (new mode, new screen, tokenizer/evaluator param flags, renderer branch; see P35 complexity item in CLAUDE.md).
- 2026-04-03: P30 STAT menu complete — `StatData_t`/`StatResults_t` + `STAT_MAX_POINTS 99` in `app_common.h`; `MODE_STAT_MENU/EDIT/RESULTS` in `CalcMode_t`; `calc_stat.h`/`calc_stat.c` (1-Var, LinReg, LnReg, ExpReg, PwrReg, SortX, SortY, Clear); `PersistBlock_t` v6 (1264 → 2060 bytes, +stat list 796 B); `ui_stat.h`/`ui_stat.c` (~470 lines, three screens + handlers); `Graph_DrawScatter`/`Graph_DrawXYLine`/`Graph_DrawHistogram` in `graph.c`; `TOKEN_STAT` dispatch + persist + screen init in `calculator_core.c`; 39-assertion `test_stat.c` host test suite; 7 test executables, 488 total assertions. **Complexity delta: `increase`** (new module, 3 modes, 3 screens, 3 graph renderers, +796 B persist; see P30 complexity item in CLAUDE.md).
- 2026-04-04: P35 complexity debt resolved — parametric Y= editor extracted from graph_ui.c into ui_param_yeq.c (325 lines) / ui_param_yeq.h (40 lines); nav_to MODE_GRAPH_PARAM_YEQ block replaced by param_yeq_nav_enter(); graph_ui.c reduced 2026 → 1743 lines (−283). Build clean, all pre-existing tests pass. **Complexity delta: decrease**.
- 2026-04-04: P29 complexity debt (draw layer portion) resolved — draw layer extracted from `graph.c` into `graph_draw.c` (120 lines) / `graph_draw.h` (54 lines); `graph.c` reduced 963 → 881 lines; `apply_draw_layer` replaced by `Graph_ApplyDrawLayer(uint16_t *dest)`; `graph.h` draw-layer section removed; CMakeLists.txt updated. Remaining P29 debt: stat renderers in `graph.c` (tightly coupled, future `graph_stat.c`), `try_execute_draw_command` in `calculator_core.c` (future `ui_draw_exec.c`). Build clean, all pre-existing tests pass. **Complexity delta: `decrease`**.
- 2026-04-04: P29 DRAW menu complete — `MODE_DRAW_MENU` in `CalcMode_t`; `ui_draw.h`/`ui_draw.c` (7-item single-list menu: ClrDraw immediate, Line(/PT-On(/PT-Off(/PT-Chg(/DrawF/Shade( insert to expression buffer); persistent draw layer in SDRAM at 0xD0080800 (320×240 RGB565, 0x0000=transparent sentinel); `Graph_DrawLayerClear/SetPixel/GetPixel/Line/DrawF/Shade` + `apply_draw_layer` + `Graph_IsVisible/MathXToPx/MathYToPx` in `graph.c`; `try_execute_draw_command`/`parse_draw_args`/`eval_draw_arg` + TOKEN_DRAW dispatch in `calculator_core.c`; pre-existing `-Werror` fixes in `graph_ui.c`, `ui_stat.h`, `ui_stat.c`; `ui_draw.h` stubs in `calculator_core_test_stubs.h`. Build clean, 6/7 test suites pass (1 pre-existing param test failure unrelated). Hardware validation pending (P29h). **Complexity delta: `increase`** (new module, draw-layer API, DRAW command parser; see P29 complexity item in CLAUDE.md).
- 2026-04-04: P31 VARS menu complete — `MODE_VARS_MENU` in `CalcMode_t`; `ui_vars.h`/`ui_vars.c` (5-tab menu: XY/Σ/LR/DIM/RNG; inserts current numeric value of selected variable into expression buffer or Y= editor); on-the-fly y-statistics (ȳ, Sy, σy, Σy, Σy², Σxy) from `stat_data`; RNG tab has 10 items with scroll; TOKEN_VARS dispatch + menu_open/menu_close + hide_all_screens in `calculator_core.c`; TOKEN_VARS in `handle_yeq_mode` (graph_ui.c); `ui_vars.c` added to CMakeLists.txt. Build clean, all 7/7 host tests pass. Hardware validation pending. **Complexity delta: `neutral`** (new module same scale as ui_draw.c; no existing file grew).
- 2026-04-04: #6 ZBox render speed — `Graph_DrawZBox` pixel writes changed from per-pixel `lv_canvas_set_px` (O(n) LVGL call chain per pixel + individual `lv_obj_invalidate` per pixel) to direct `graph_buf[]` uint16_t writes + single `lv_obj_invalidate(graph_canvas)` at end. Eliminates ~200+ redundant per-pixel LVGL calls per arrow key event. Build clean, 7/7 tests pass. **Complexity delta: neutral**.
- 2026-04-15: UI super-module Phase 2 extractions — (1) `handle_prgm_running()` moved from `prgm_exec.c` to `ui_prgm.c` (correct home; was an architectural misplacement); (2) `ui_mode.h`/`ui_mode.c` extracted from `calculator_core.c` — MODE screen state, LVGL init, display update, and `handle_mode_screen`; `ui_mode_open()` replaces inline TOKEN_MODE handling; (3) `ui_input.h`/`ui_input.c` extracted — all normal-mode expression input handlers (`expr_insert_str/char/prepend`, `handle_digit_key`, `handle_arithmetic_op`, `handle_function_insert`, `handle_sto_key/pending`, `handle_clear_key`, `handle_normal_graph_nav`, `handle_normal_mode`); `calculator_core.c` reduced 2502 → 2131 lines (−371); `calc_internal.h` updated with new externs and declarations; `calculator_core_test_stubs.h` updated with new stubs; both CMakeLists.txt files updated; pre-existing `test_param` UB fixed (use `√` Unicode glyph instead of unrecognized `"sqrt"` string in error-propagation test). 8/8 host test suites pass. **Complexity delta: `decrease`** (handler code now in focused single-responsibility files; calculator_core.c closer to pure coordinator role).
- 2026-04-15: P32 Y-VARS menu complete — `MODE_YVARS_MENU` in `CalcMode_t`; `TOKEN_Y1–Y4` in `keypad_map.h`; `MATH_VAR_Y1–Y4` + `Calc_RegisterYEquations` + ShuntingYard/ImplicitMulPass handling in `calc_engine.c`; `ui_yvars.h`/`ui_yvars.c` (3-tab menu Y/ON/OFF; Y tab inserts "Y₁"–"Y₄" strings; ON/OFF tabs toggle `graph_state.enabled[]` directly); reentrancy guard prevents infinite recursion in self-referencing Y equations; TOKEN_Y_VARS dispatch + menu_open/menu_close/hide_all_screens in `calculator_core.c`; TOKEN_Y_VARS in `handle_yeq_mode` (graph_ui.c); `ui_yvars.c` added to CMakeLists.txt; stubs in `calculator_core_test_stubs.h`. Build clean, 20/20 host tests pass (test_yvars), all 8/8 test suites pass (1 pre-existing param failure unrelated). Hardware validation pending (P32h). **Complexity delta: neutral** (new module follows established ui_vars.c pattern; no existing file grew significantly).
- 2026-04-15: UI super-module Phase 3 extractions — (1) MATH/NUM/HYP/PRB and TEST menu code extracted from `calculator_core.c` into `ui_math_menu.h`/`ui_math_menu.c` (481 lines) — private types (`MenuItem_t`, `MathMenuState_t`, `TestMenuState_t`), statics, label arrays, data tables, and all handlers; `menu_open`/`menu_close` updated to delegate to new `math_menu_open/close`/`test_menu_open/close` functions; (2) `try_execute_draw_command` + `parse_draw_args` + `eval_draw_arg` moved from `calculator_core.c` into `ui_draw.c` (their natural home — all required headers already present); `try_execute_draw_command` added to `ui_draw.h`; `calculator_core.c` reduced 2131 → 1591 lines (−540); `calc_internal.h` updated; `calculator_core_test_stubs.h` updated with inline stubs (`math_menu_open/close`, `test_menu_open/close` set `current_mode`); `test_normal_mode.c` updated with `ui_math_screen`/`ui_test_screen` definitions; both CMakeLists.txt updated (test build uses stubs — `ui_math_menu.c` excluded); TECHNICAL.md and ARCHITECTURE.md updated. 8/8 host test suites pass, 91/91 check_sync checks pass. **Complexity delta: `decrease`** (−540 lines from coordinator; MATH/TEST menus now in focused single-responsibility file alongside other extracted menus).
- 2026-04-15: graph_ui_range.c extraction — RANGE field editor and ZOOM FACTORS editor screens extracted from `graph_ui.c` into `graph_ui_range.h`/`graph_ui_range.c` (718 lines). Moved: `FieldEditor_t`, `field_editor_handle`, `handle_range_mode`, `handle_zoom_factors_mode`, all RANGE/ZOOM-FACTORS state/LVGL statics, all helpers, persist accessors, `ui_graph_range_screen`/`ui_graph_zoom_factors_screen` definitions. `zoom_menu_reset` made non-static (called from `graph_ui_range.c`); `zoom_enter_factors` updated to call `zoom_factors_nav_enter()`; `nav_to(MODE_GRAPH_RANGE)` updated to call `range_nav_enter()`; `zoom_execute_item` updated to use `graph_ui_get_zoom_x/y_fact()` accessors. `graph_ui.h` updated (includes `graph_ui_range.h`, removes moved declarations, adds `zoom_menu_reset` declaration). graph_ui.c reduced 1743 → 1131 lines (−612). CMakeLists.txt and TECHNICAL.md/ARCHITECTURE.md updated. 8/8 host tests pass, 95/95 check_sync checks pass. **Complexity delta: `decrease`**.
- 2026-04-15: ui_prgm.c sub-menu extraction (INTERFACE_REFACTOR_PLAN Item 1) — CTL, I/O, and EXEC sub-menu handlers extracted from `ui_prgm.c` into three new modules: `ui_prgm_ctl.h`/`ui_prgm_ctl.c`, `ui_prgm_io.h`/`ui_prgm_io.c`, `ui_prgm_exec.h`/`ui_prgm_exec.c`. Each follows the `ui_math_menu.h` pattern: non-static screen pointer extern in header, `ui_init_*_screen(parent)` + `ui_prgm_*_reset_and_show()` + `ui_update_*_display()` + `handle_prgm_*_menu()` API. Shared helpers (`prgm_editor_insert_str`, `prgm_flatten_to_store`, `prgm_submenu_return_to_editor`, `prgm_submenu_tab_switch`) made non-static and declared in `ui_prgm.h`. `ui_prgm.c` reduced 1652 → 1276 lines. TECHNICAL.md/ARCHITECTURE.md/CLAUDE.md updated. 8/8 host test suites pass. **Complexity delta: `neutral`** (pure extraction; no new logic).
- 2026-04-16: Firmware build fixes — three pre-existing compilation errors from Item 1: (1) `ui_mode.c` missing `#include "ui_palette.h"` (COLOR_* undeclared); (2) `ui_math_menu.h` missing `#include "lvgl.h"` (lv_obj_t unknown type); (3) `ui_math_menu.c` missing `#include <stdio.h>` (snprintf implicit); (4) firmware CMakeLists.txt missing `expr_buffer.c`, `ui_prgm_ctl.c`, `ui_prgm_io.c`, `ui_prgm_exec.c` (linker errors); (5) `handle_prgm_running` in `ui_prgm.c` accessing `static` variables from `prgm_exec.c` directly after move — replaced with accessor API (`prgm_is_waiting_input`, `prgm_get_input_var`, `prgm_clear_input_wait`, `prgm_reset_execution_state`). Firmware builds 0 errors. 9/9 host tests pass.
- 2026-04-16: ExprBuffer_t wrapper (INTERFACE_REFACTOR_PLAN Item 2) — `MAX_EXPR_LEN` moved to `app_common.h`; `ExprBuffer_t` struct (buf/len/cursor) + 5 accessor API added to `expr_util.h`; `expr_buffer.c` new file (zero LVGL/HAL dependencies); `calculator_core.c` three raw globals replaced by `ExprBuffer_t expr`; `calc_internal.h` updated to single extern; `ui_input.c`, `ui_prgm.c`, `ui_draw.c`, `prgm_exec.c` migrated to `expr.buf/len/cursor`; test stubs updated; `test_expr_buffer.c` added (8 groups / 48 assertions); `expr_util.c` off-by-one bug fixed in `ExprUtil_InsertChar` overflow guard (`> max_len` → `>= max_len`). 9/9 host test suites pass, 105/105 check_sync checks pass. **Complexity delta: `neutral`** (pure structural wrapping; no new logic).
- 2026-04-16: MenuState_t navigation helpers (INTERFACE_REFACTOR_PLAN Item 3) — `MenuState_t` struct + 5 helpers (`MoveUp`, `MoveDown`, `PrevTab`, `NextTab`, `DigitToIndex`) + `AbsoluteIndex` inline in new `menu_state.h`/`menu_state.c` (zero LVGL/HAL dependencies); `VarsMenuState_t` bespoke struct removed from `ui_vars.h`; `ui_vars.c` migrated to `MenuState_t` with UP/DOWN using `MenuState_MoveUp/Down`, digit shortcuts using `MenuState_DigitToIndex`; `calculator_core.c` field names updated (`item_cursor` → `cursor`, `scroll_offset` → `scroll`); test stubs (`calculator_core_test_stubs.h`, `test_normal_mode.c`) updated; `test_menu_state.c` added (5 groups / 43 assertions); TODO migration notes added to `ui_stat.c`, `ui_math_menu.c`, `ui_matrix.c`, `ui_yvars.c`, `ui_draw.c`. 10/10 host test suites pass, 694 total assertions, 108/108 check_sync checks pass. **Complexity delta: `neutral`** (pure structural unification; no logic changes).
- 2026-04-16: ZOOM menu extraction (INTERFACE_REFACTOR_PLAN Item 4) — `ui_graph_zoom.h`/`ui_graph_zoom.c` new files; 10 functions extracted from `graph_ui.c` (`ui_init_zoom_screen`, `apply_zoom_preset`, `ui_update_zoom_display`, `zoom_menu_reset`, `zoom_show_graph`, `zoom_enter_factors`, `zoom_scale_view`, `zoom_execute_item`, `handle_zoom_mode`); `graph_ui.c` 1131 → 862 lines; `ui_update_zoom_display` / `handle_zoom_mode` removed from `graph_ui.h`; `calculator_core.c` includes `ui_graph_zoom.h`. `zoom_enter_zbox` kept in `graph_ui.c` — owns `s_zbox` state. **Complexity delta: `neutral`**.
- 2026-04-16: graph_state ownership documentation (INTERFACE_REFACTOR_PLAN Item 5) — ownership comment block added above `GraphState_t` in `app_common.h`; "State Ownership" section added to `docs/TECHNICAL.md` covering `graph_state`, `calc_variables[]`, `calc_matrices[]`, `stat_data`, and `ans`. No code changes. **Complexity delta: `neutral`**.
- 2026-04-16: PersistBlock_t sub-struct split design (INTERFACE_REFACTOR_PLAN Item 6) — "Persist Migration Design" section added to `docs/TECHNICAL.md`: current flat layout problem, proposed sub-struct layout (`GraphPersist_t`, `StatPersist_t`, `MatrixPersist_t`, `PrgmPersist_t`, `ModePersist_t`), field assignment table, per-section migration switch pseudocode, adoption constraint (atomic at next version bump), and implementation order. No code changes. All 6 INTERFACE_REFACTOR_PLAN items now complete. **Complexity delta: `neutral`**.
- 2026-04-16: COUPLING_REFACTOR T2 (`g_prgm_store` accessor API) — 7 accessor functions added to `prgm_exec.h`/`prgm_exec.c` (`Prgm_GetName`, `Prgm_GetBody`, `Prgm_IsSlotOccupied`, `Prgm_SetName`, `Prgm_AppendLine`, `Prgm_SetBody`, `Prgm_ClearSlot`); all direct `g_prgm_store` field writes migrated in `ui_prgm.c` (12 sites) and `ui_prgm_exec.c` (2 sites); both test stubs (`prgm_exec_test_stubs.h`, `calculator_core_test_stubs.h`) updated; `test_prgm_exec.c` and `test_normal_mode.c` reset helpers migrated; `extern ProgramStore_t g_prgm_store` removed from `prgm_exec.h`; `g_prgm_store` is now an exclusively-internal variable in `prgm_exec.c` (embedded) / test TU (HOST_TEST). 10/10 ctest pass, firmware builds 0 errors, check_sync 111/111. **Complexity delta: `neutral`** (7 thin wrappers; no new control flow).
- 2026-04-16: COUPLING_REFACTOR T3 (ANS getter/setter) — `calculator_core.h` new public header; 4 functions added (`Calc_GetAns`, `Calc_GetAnsIsMatrix`, `Calc_SetAnsScalar`, `Calc_SetAnsMatrix`); `ans` and `ans_is_matrix` made static in `calculator_core.c` (conditional on `#ifndef HOST_TEST`); `format_calc_result` signature simplified (removed `float *ans_out` out-parameter — function now writes `ans` directly as the owning TU); 6 migration sites across `ui_input.c`, `ui_prgm.c`, `ui_draw.c`, `prgm_exec.c` (Calc_Evaluate read sites + write sites); `extern float ans` / `extern bool ans_is_matrix` removed from `calc_internal.h`; `prgm_exec_test_stubs.h` updated with inline ANS stubs; `calculator_core_test_stubs.h` updated with 4 function declarations; `TECHNICAL.md` ans/ans_is_matrix ownership section updated. Direct field writes outside `calculator_core.c` are now compile errors. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `neutral`** (4 one-liner functions; signature simplification removes one out-pointer; call sites net no new lines).
- 2026-04-16: COUPLING_REFACTOR T1 (graph state accessor layer) — `graph_state` moved from `calculator_core.c` to `graph.c` (now `static`); 13 accessor functions added (`Graph_GetState`, `Graph_SetWindow`, `Graph_SetEquationEnabled`, `Graph_SetParamEnabled`, `Graph_SetParamMode`, `Graph_SetParamWindow`, `Graph_SetGridOn`, `Graph_SetActive`, `Graph_GetEquationBuf`, `Graph_GetParamEquationXBuf`, `Graph_GetParamEquationYBuf`); `GRAPH_EQUATION_BUF_LEN` constant added; 9 modules migrated away from direct extern field writes (`graph_ui.c`, `graph_ui_range.c`, `ui_param_yeq.c`, `ui_mode.c`, `ui_yvars.c`, `ui_graph_zoom.c`, `ui_vars.c`, `ui_input.c`, `calculator_core.c`); `extern GraphState_t graph_state` removed from `app_common.h`; `ui_graph_zoom.c` and `menu_state.c` added to firmware CMakeLists.txt (were missing); `#include "graph.h"` added to `ui_vars.c`, `ui_param_yeq.c`, `graph_ui_range.c`; host-test stubs updated with `GRAPH_EQUATION_BUF_LEN`. Direct field writes outside `graph.c` are now compile errors. API/header design A → A+. 10/10 ctest pass, firmware builds 0 errors, check_sync 111/111. **Complexity delta: `neutral`**.
- 2026-04-17: COUPLING_REFACTOR T7 (`Execute_Token` dispatch table) — `ModeHandler_t` function-pointer typedef + `ModeEntry_t` struct typedef added; 3 thin wrappers (`dispatch_matrix_menu`, `dispatch_matrix_edit`, `dispatch_stat_menu`) capture non-uniform signatures; `k_mode_handlers[]` static const table with 22 entries replaces 22-entry sequential if-chain; `_Static_assert(ARRAY_SIZE(k_mode_handlers) == 22, ...)` guards table integrity at compile time; ALPHA_LOCK compound cases (`MODE_PRGM_NEW_NAME`, `MODE_PRGM_EDITOR`) kept as explicit checks outside the table; `MODE_PRGM_RUNNING` always-return case kept before the table. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `decrease`** (Execute_Token dispatcher reduced from 24 branches to 5; table is pure declarative data).
- 2026-04-17: COUPLING_REFACTOR T6 (`menu_open`/`menu_close` module delegation) — `Matrix_MenuOpen/Close`, `Stat_MenuOpen/Close`, `Draw_MenuOpen/Close`, `Vars_MenuOpen/Close`, `Yvars_MenuOpen/Close` added to their owning modules (10 one-liner functions each); inline state-init blocks removed from `menu_open()`/`menu_close()` in `calculator_core.c`; both dispatcher switches now consist entirely of single-line delegation calls; corresponding inline stubs added to `calculator_core_test_stubs.h`; `calculator_core.c` reduced 1591 → 1544 lines. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `decrease`** (ten multi-line blocks → ten one-liners; logic moved to owning modules).
- 2026-04-17: COUPLING_REFACTOR T5 (Mode transition via `Calc_SetMode()`) — 4 accessor functions added to `calculator_core.h`/`calculator_core.c` (`Calc_SetMode`, `Calc_SetReturnMode`, `Calc_GetMode`, `Calc_GetReturnMode`); `current_mode` and `return_mode` made `static` in `calculator_core.c`; `extern CalcMode_t current_mode/return_mode` removed from `calc_internal.h`; ~20 direct write/read sites migrated across 13 files (`ui_mode.c`, `ui_math_menu.c`, `ui_matrix.c`, `ui_stat.c`, `ui_draw.c`, `ui_vars.c`, `ui_yvars.c`, `ui_prgm.c`, `graph_ui.c`, `graph_ui_range.c`, `ui_graph_zoom.c`, `ui_param_yeq.c`, `prgm_exec.c`); CLAUDE.md gotcha #21 MODE_STO pattern and gotcha #12 TRACE fallthrough preserved unchanged; `ui_draw.c` and `ui_vars.c` had only struct-field `return_mode` accesses — no changes needed. Direct `current_mode`/`return_mode` assignments outside `calculator_core.c` are now compile errors. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `neutral`** (4 one-liner functions; pure mechanical substitution).
- 2026-04-17: COUPLING_REFACTOR T4 (LVGL screen show/hide API) — 16 screen pointers made `static` across their owning modules; extern screen pointer block removed from `calc_internal.h`; extern declarations removed from `graph_ui.h`, `graph_ui_range.h`, `ui_graph_zoom.h`, `ui_param_yeq.h`, `ui_matrix.h`, `ui_stat.h`, `ui_draw.h`, `ui_vars.h`, `ui_yvars.h`, `ui_math_menu.h`, `ui_mode.h`; show/hide/IsVisible one-liner functions added to all 11 owning modules (`Graph_ShowYeqScreen/HideYeqScreen/IsYeqScreenVisible`, `Graph_ShowRangeScreen/HideRangeScreen/IsRangeScreenVisible`, `Graph_ShowZoomFactorsScreen/HideZoomFactorsScreen/IsZoomFactorsScreenVisible`, `Zoom_ShowScreen/HideScreen`, `ParamYeq_ShowScreen/HideScreen`, `Matrix_ShowMenuScreen/HideMenuScreen/ShowEditScreen/HideEditScreen/IsEditScreenVisible`, `Stat_ShowMenuScreen/HideMenuScreen/ShowEditScreen/HideEditScreen/ShowResultsScreen/HideResultsScreen`, `Draw_ShowScreen/HideScreen`, `Vars_ShowScreen/HideScreen`, `Yvars_ShowScreen/HideScreen`, `Math_ShowScreen/HideScreen`, `Test_ShowScreen/HideScreen`, `Mode_HideScreen`); cross-module zoom-screen refs in `graph_ui.c` and `graph_ui_range.c` replaced with `Zoom_ShowScreen/HideScreen`; `hide_all_screens()`, `menu_open()`, `menu_close()`, `cursor_timer_cb()`, `ui_update_status_bar()` in `calculator_core.c` fully migrated; all new stubs added to `calculator_core_test_stubs.h`. Direct `lv_obj_*` calls on other modules' screen pointers are now compile errors in `calculator_core.c`. 10/10 ctest pass, firmware builds 0 errors, check_sync 112/112. **Complexity delta: `neutral`** (one-liner wrappers; no new logic or data structures).
- 2026-04-17: COUPLING_REFACTOR T8 (Extract `calc_history.c`) — history ring buffer state (`s_history[]`, `s_history_count`, `s_history_recall_offset`, `s_matrix_scroll_focus`, `s_matrix_scroll_offset`) moved from `calculator_core.c` statics into new `App/Src/calc_history.c` / `App/Inc/calc_history.h`; 13-function `CalcHistory_*` API (`Init`, `Commit`, `Clear`, `RecallUp`, `RecallDown`, `GetCount`, `GetRecallOffset`, `ResetRecallOffset`, `GetEntry`, `GetExprForRecall`, `ResetMatrixScroll`, `GetMatrixScrollFocus`, `GetMatrixScrollOffset`, `SetMatrixScrollOffset`); `CalcHistory_UpdateDisplay()` declared in `calc_history.h` but defined in `calculator_core.c` (needs private `disp_rows[]`); extern history state removed from `calc_internal.h` (replaced by `#include "calc_history.h"`); `HISTORY_LINE_COUNT`, `MAX_RESULT_LEN`, `MATRIX_RING_COUNT`, `HistoryEntry_t` moved to `calc_history.h`; 6 call sites migrated (`calculator_core.c`, `ui_input.c`, `prgm_exec.c` ×3, `ui_prgm.c`); test infra: `calc_history.c` added to `test_normal_mode` target; `prgm_exec_test_stubs.h` updated with `CalcHistory_*` + `Calc_SetMode`/`Calc_GetMode` inline stubs; `calculator_core_test_stubs.h` updated (stale `ui_mode_screen` extern removed; `current_mode`/`return_mode` made non-static in HOST_TEST builds to match `ans` pattern); `TECHNICAL.md` + `ARCHITECTURE.md` updated with `calc_history.c/h`. 10/10 ctest pass (694 assertions), firmware builds 0 errors, check_sync 114/114. **Complexity delta: `neutral`** (~150 lines moved; no new control flow).
- 2026-04-17: COUPLING_REFACTOR T10 (first module — `ui_yvars.c`) — `YVarsMenuState_t` bespoke struct (tab/item_cursor/return_mode) removed from `ui_yvars.h`; `ui_yvars.c` migrated to `MenuState_t` from `menu_state.h`; `item_cursor` field renamed to `cursor`; manual UP/DOWN bounds-check logic replaced with `MenuState_MoveUp/Down(s, total, MENU_VISIBLE_ROWS)`; `tab_move` calls updated (`&s->item_cursor` → `&s->cursor`); `Yvars_MenuOpen/Close` updated to initialise `cursor` and `scroll` explicitly; `YVarsMenuState_t` typedef removed from `calculator_core_test_stubs.h`; definition in `test_normal_mode.c` updated to `MenuState_t yvars_menu_state = {0}`. Four remaining modules (`ui_stat.c`, `ui_math_menu.c`, `ui_matrix.c`, `ui_draw.c`) retain their bespoke types with TODO notes. 10/10 ctest pass (694 assertions), firmware builds 0 errors, check_sync 115/115. **Complexity delta: `decrease`** (bespoke typedef removed; duplicate UP/DOWN bounds-check code eliminated; no new logic).
- 2026-04-17: COUPLING_REFACTOR T9 (Move `BuildPersistBlock`/`ApplyPersistBlock` to `persist.c`) — `Calc_BuildPersistBlock` and `Calc_ApplyPersistBlock` (~113 lines) removed from `calculator_core.c` and reimplemented as `Persist_BuildBlock(void) → PersistBlock_t` and `Persist_ApplyBlock(const PersistBlock_t *)` in `persist.c`; `persist.c` gains 5 new includes inside `#ifndef HOST_TEST` (`calc_engine.h`, `calculator_core.h`, `ui_mode.h`, `graph.h`, `graph_ui_range.h`); `Persist_BuildBlock` return-by-value replaces output-pointer pattern; `Calc_GetAngleDegrees`/`Calc_SetAngleDegrees` added to `calculator_core.h`/`calculator_core.c` (2 one-liners) so `persist.c` can restore angle mode without including `calc_internal.h`; old `Calc_*` names removed from `persist.h` and `persist.c`; call site in `Execute_Token` (save path) and `StartCalcCoreTask` (restore path) updated; `calculator_core_test_stubs.h` updated with `Persist_BuildBlock`/`Persist_ApplyBlock` declarations; `test_normal_mode.c` gains two no-op stub definitions; `ui_mode.h` comment updated; `docs/TECHNICAL.md` and `docs/POWER_MANAGEMENT.md` updated; `calculator_core.c` reduced 1544 → 1453 lines; `persist.c` grew 146 → 281 lines. 10/10 ctest pass (694 assertions), firmware builds 0 errors, check_sync 115/115. **Complexity delta: `neutral`** (~113 lines relocated; no new control flow; 2 new one-liner accessors).

---

## Completed Features

| Feature | Log date | Notes |
|---|---|---|
| **Build** | — | Successful (fixed implicit fallthrough in `graph_ui.c`; startup crash and trace freeze in `graph.c`) |
| **Flash** | — | Successful using OpenOCD to STM32F429I-DISC1 |
| ZBox rubber-band zoom | 2026-03-20 | Final validation on hardware pending |
| UTF-8 cursor navigation | 2026-03-21 | 12 test groups; `expr_util.c` extracted |
| Persist checksums/HAL | 2026-03-21 | FLASH sector 7 guard added; versioned header |
| Graph UI extraction | 2026-03-21 | Removed 1.5k LOC from `calculator_core.c` (P2) |
| Project Update Procedure | 2026-03-21 | Canonical sync rules + workflow automated |
| Global Hard QUIT | 2026-03-22 | 2nd+CLEAR hard exit to main screen implemented |
| Y= Toggle | 2026-03-22 | Equation enable/disable toggle from Y= editor implemented |

---

## Resolved Items

| Item | Summary | Date |
|---|---|---|
| P5 | `const` on `TI81_LookupTable` | 2026-03-20 |
| P4 | `ui_palette.h` — 14 named colour constants; inline hex literals replaced | 2026-03-21 |
| P9 | `mat_det` confirmed iterative; tracker entry was incorrect | 2026-03-21 |
| P11 | Duplicate `#include` directives removed from `calculator_core.c` | 2026-03-21 |
| P6 | `-Werror` enabled for App sources | 2026-03-21 |
| P8 | Float printf runtime guard in `App_DefaultTask_Run` | 2026-03-21 |
| P2 | `graph_ui.c` extracted — `calculator_core.c` −66% | 2026-03-21 |
| P16 | FLASH sector map added to `docs/TECHNICAL.md` | 2026-03-21 |
| P12 | Architecture diagram — Mermaid added to `docs/ARCHITECTURE.md` | 2026-03-21 |
| P13 | `docs/TESTING.md` created | 2026-03-21 |
| P17 | `docs/TROUBLESHOOTING.md` created | 2026-03-21 |
| Open-source governance | `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, issue/PR templates, README badges | 2026-03-21 |
| P15 | Expression pipeline walkthrough added to `docs/TECHNICAL.md` | 2026-03-22 |
| P18 | All 10 `CODE_REVIEW_PENDING` items resolved — function complexity C+ → B | 2026-03-22 |
| P19 | `prgm_execute_line` dispatch table — 22 handlers, body 495 → ~30 lines | 2026-03-22 |
| P20 | prgm exec host suite — 121 tests, `prgm_run_loop` bug fixed; Testing B+ → A- | 2026-03-22 |
| P21–P24 | Promoted from retired `CODE_REVIEW_PENDING.md` to CLAUDE.md next priorities | 2026-03-25 |
| P25 | `docs/PRGM_COMMANDS.md` rewritten — 8-CTL/5-IO spec, removed 6 unsupported commands | 2026-04-01 |
| `handle_prgm_editor` split | Extracted 3 static helpers; dispatcher 152 → 40 lines; `ui_prgm.c` | 2026-04-01 |
| `handle_normal_mode` tests | 104-test suite; `calculator_core_test_stubs.h`; HOST_TEST guards; self-copy bug fixed | 2026-04-02 |
| P3 Phase 3 | State params for 4 handlers; `MatrixMenuState_t` public; history buffer 32→1; ~7 KB RAM freed | 2026-04-02 |
| History spec alignment | `HISTORY_LINE_COUNT` 32→1, `MATRIX_RING_COUNT` 8→1; UP/DOWN scroll and `2nd+ENTRY` deviation removed | 2026-04-02 |
| Item 18 | Y= row labels → Y₁–Y₄; TOKEN_X_INV display-only → ⁻¹ U+E001; `^-1` expression insertion kept (Option B); VARS/Y-VARS strings deferred | 2026-04-02 |
| `handle_prgm_menu` split | Extracted `handle_erase_confirm`, `enter_exec_tab`, `enter_edit_tab`, `enter_erase_tab`; dispatcher ~120 → ~50 lines; zero logic change; `graph_ui.c` implicit-fallthrough fixed | 2026-04-02 |
| P34 | Parametric graphing — `MATH_VAR_T`, `Calc_PrepareParamEquation`/`Calc_EvalParamEquation`, `GraphState_t` param fields, persist v5, `Graph_RenderParametric`/`Graph_InvalidateCache`, param Y= screen + 9-field RANGE + `handle_param_yeq_mode`, `TOKEN_X_T` + MODE row 4 + `MODE_GRAPH_PARAM_YEQ` dispatch; 28-test `test_param.c`; hardware validation pending (P35h) | 2026-04-02 |
| P30 | STAT menu — `StatData_t`/`StatResults_t` in `app_common.h`; `calc_stat.c` (1-Var, LinReg, LnReg, ExpReg, PwrReg, SortX, SortY, Clear); persist v6 (+796 B stat list); `ui_stat.c` (3 screens: tab-menu, DATA editor, results); `Graph_DrawScatter`/`XYLine`/`Histogram` in `graph.c`; `TOKEN_STAT` dispatch in `calculator_core.c`; 39-test `test_stat.c`; hardware validation pending (P30h) | 2026-04-03 |
| P29 | DRAW menu — `MODE_DRAW_MENU`; `ui_draw.h`/`ui_draw.c` (7-item menu: ClrDraw immediate + 6 expression-insert commands); persistent draw layer in SDRAM (0xD0080800, 150 KB, transparent-sentinel); `Graph_DrawLayer*` + `apply_draw_layer` + coordinate helpers in `graph.c`; `try_execute_draw_command` + TOKEN_DRAW dispatch in `calculator_core.c`; HOST_TEST stubs added; hardware validation pending (P29h) | 2026-04-04 |
| P31 | VARS menu — `MODE_VARS_MENU`; `ui_vars.h`/`ui_vars.c` (5-tab: XY/Σ/LR/DIM/RNG, 10-item RNG tab with scroll); inserts current value of selected variable; on-the-fly y-stats from `stat_data`; TOKEN_VARS in `handle_normal_mode` + `handle_yeq_mode`; hardware validation pending | 2026-04-04 |
| P32 | Y-VARS menu — `MODE_YVARS_MENU`; `TOKEN_Y1–Y4`; `MATH_VAR_Y1–Y4` + `Calc_RegisterYEquations` + ShuntingYard/ImplicitMulPass support in `calc_engine.c`; `ui_yvars.h`/`ui_yvars.c` (3-tab Y/ON/OFF; Y inserts "Y₁"–"Y₄" ref strings; ON/OFF toggle `graph_state.enabled[]`); reentrancy guard; TOKEN_Y_VARS dispatch; 20-test `test_yvars.c`; hardware validation pending (P32h) | 2026-04-15 |
| REFACTOR Item 1 | ui_prgm.c sub-menu extraction — CTL/IO/EXEC into 3 new modules; ui_prgm.c 1652 → 1276 lines | 2026-04-15 |
| REFACTOR Item 2 | ExprBuffer_t wrapper — 3 raw globals → struct; expr_buffer.c; test_expr_buffer.c (48 assertions) | 2026-04-16 |
| REFACTOR Item 3 | MenuState_t navigation helpers — menu_state.h/menu_state.c; ui_vars.c proof-of-concept migration; VarsMenuState_t removed; test_menu_state.c (43 assertions); TODO notes added to 5 remaining menus | 2026-04-16 |
| REFACTOR Item 4 | ZOOM menu extraction — ui_graph_zoom.h/ui_graph_zoom.c; graph_ui.c 1131 → 862 lines; 10 functions moved | 2026-04-16 |
| REFACTOR Item 5 | graph_state ownership documentation — comment block in app_common.h; State Ownership section in TECHNICAL.md | 2026-04-16 |
| REFACTOR Item 6 | PersistBlock_t sub-struct split design — Persist Migration Design section in TECHNICAL.md; proposed GraphPersist_t/StatPersist_t/MatrixPersist_t/PrgmPersist_t/ModePersist_t layout documented; adopt at next version bump | 2026-04-16 |
| INTERFACE_REFACTOR_PLAN | All 6 items complete — structural refactoring milestone finished | 2026-04-16 |
| COUPLING_REFACTOR T1 | graph_state accessor layer — static in graph.c; 13 accessors; extern removed from app_common.h; 9 modules migrated; ui_graph_zoom.c + menu_state.c added to CMakeLists; API/header design A → A+ | 2026-04-16 |
| COUPLING_REFACTOR T2 | g_prgm_store accessor API — 7 accessors (GetName/GetBody/IsSlotOccupied/SetName/AppendLine/SetBody/ClearSlot); extern removed from prgm_exec.h; ui_prgm.c + ui_prgm_exec.c + both test stubs migrated; API/header design remains A+ | 2026-04-16 |
| COUPLING_REFACTOR T7 | Execute_Token dispatch table — 22-entry `k_mode_handlers[]` replaces sequential if-chain; 3 thin wrappers for non-uniform signatures; `_Static_assert` guards count; ALPHA_LOCK compound cases kept explicit; complexity decrease | 2026-04-17 |
| COUPLING_REFACTOR T10 (ui_yvars) | MenuState_t retrofit — YVarsMenuState_t removed; item_cursor→cursor; MenuState_MoveUp/Down replaces manual bounds checks; test stubs updated; 4 modules remain | 2026-04-17 |
| COUPLING_REFACTOR T5 | Mode transition via Calc_SetMode() — 4 accessors in calculator_core.h; current_mode/return_mode static in calculator_core.c; extern removed from calc_internal.h; 13 files migrated; direct writes outside calculator_core.c are now compile errors; API/header design remains A+ | 2026-04-17 |
| #6 ZBox render speed | `Graph_DrawZBox`: per-pixel `lv_canvas_set_px` → direct `graph_buf[]` writes + single `lv_obj_invalidate`; eliminates ~200+ redundant LVGL calls per keypress (hardware waiver: mechanical rewrite — same pixel values, same bounds checks, same visual output; 2026-04-04) | 2026-04-04 |

---

## Milestone Reviews

| Date | Notes |
|---|---|
| 2026-03-20 | Initial review; P1–P10 logged; rating 60–70% |
| 2026-03-21 | Testing D → C → B → B+ across multiple sessions. Full quality pass; header audit A-grade. Rating 80–88%. |
| 2026-03-21 | Sessions 6–10: P2/P6/P8/P12/P13/P16/P17 resolved. `-Werror` enabled; Architecture/Testing/Troubleshooting docs created. Rating 88–92%. |
| 2026-03-22 | Sessions 11–13: Y= toggle, persist v4, matrix history display. RAM 82.44%. |
| 2026-03-22 | Session 15: P18 — function complexity C+ → B. Rating 92–94%. |
| 2026-03-22 | Sessions 16–18: Graph postfix cache; matrix ring buffer; LVGL heap → SDRAM. RAM 48.49%. |
| 2026-03-22 | Sessions 19–23: PRGM feature-complete (~95%); dispatch table (P19); 121-test exec suite (P20). Testing B+ → A-. Rating 93–95%. |
| 2026-03-25 | Sessions 26–28: PRGM spec alignment −44 tests (suite 378/378); manual test plan 40 → 50; `CODE_REVIEW_PENDING.md` retired; P21–P24 promoted. |
| 2026-03-26 | `QUALITY_TRACKER.md` renamed to `MAINTENANCE_STANDARDS.md`; restructured as process document; history moved here. |
| 2026-04-01 | P28 complete: `cursor_place()` → `cursor_render()`. All 7 cursor-update functions now pass explicit `visible`/`mode`/`insert` params; no global state read. `MODE_STO` synthetic enum added. API/header design A- → A. Rating 90–92%. Complexity delta: **decrease**. |
| 2026-04-01 | Refactor+docs batch (session 30): P21–P27 static helper extractions across graph_ui.c/calculator_core.c/app_init.c; MAINTENANCE_STANDARDS.md/TECHNICAL.md stale-reference cleanup; P1 property-based tests (sin²+cos²=1 ×1000, Calc_FormatResult sci-notation boundaries). Testing A- → A. Suite 412/412. Rating 91–93%. |
| 2026-04-02 | `handle_normal_mode` host tests: 104 tests / 10 groups; calculator_core_test_stubs.h stub layer; self-copy bug fixed in commit_history_entry. Suite 412 → 516. Testing remains A. Rating 91–93%. |
| 2026-04-02 | P3 Phase 3 complete: state params for 4 handlers; `MatrixMenuState_t` public; history buffer 32→1; ~7 KB RAM freed; TI-81 history deviation resolved. Complexity delta: **decrease**. Rating 91–93%. |
| 2026-04-16 | INTERFACE_REFACTOR_PLAN all 6 items complete. No scorecard dimension changed (all items complexity-neutral; code organisation targets not met: ui_prgm.c 1268 lines vs 1000 target, graph_ui.c 862 lines vs 800 target). Follow-on work logged in CLAUDE.md: MenuState_t retrofit for 5 remaining menus, graph_state accessor layer, PersistBlock_t sub-struct adoption at next version bump. INTERFACE_REFACTOR_PLAN.md retired. Rating 91–93% (unchanged). |
| 2026-04-16 | COUPLING_REFACTOR T1 complete. `graph_state` encapsulated as static in `graph.c`; all external writes via `Graph_*` accessor functions; `extern GraphState_t graph_state` removed from `app_common.h`. API/header design A → A+. Reviewer: Claude Code (claude-sonnet-4-6). |
